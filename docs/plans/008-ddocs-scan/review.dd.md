<!-- GENERATED by `ddocs build` — do not hand-edit; regenerate from the .dd.json. -->
# Review — 008 ddocs scan

**Schema**: builder/review · **Source**: review.dd.json · **Sections**: 2

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | Review — 008 ddocs scan |
| reviewer | pij-regular-rodent |
| model | github-copilot/gpt-5.6-sol |
| round | 1 |
| date | 2026-08-28 |
| plan | [meta](plan.dd.md#meta) |
| note | VERDICT: REQUEST_CHANGES. Reviewed commit 2c2ff24de8e1bcb29fd528138e0e61d95f687e3c against merge-base c6ad8b4d7b0710105b6b805bffe7027feb46b357. Observed cargo 1.95.0 and ddocs 0.1.0; coder receipts independently record node 24.7.0. Targeted ddoc tests passed (26 core, 10 parser, 13 daemon, 1 CLI eval), ddocs doctor reported 41 discovered / 34 swept / 0 findings, and harness checks passed all 9 gates.<br><br>AC JUDGMENT (unit making it observable): ac-0001 MET (u1 discovery/parser + u4 search; isolated branch stack returned rows and 30 generated siblings were skipped); ac-0002 MET (u1); ac-0003 NOT MET universally (u1/u3/u4; duplicate-content paths make get fail, fn-0001); ac-0004 MET (u1 raw-hash reorder fixture + existing embed/summarize spend guards + PM count proof); ac-0005 NOT MET at the agent surface (u1/u2 produce EmbedBasis, u4 drops it, fn-0005); ac-0006 PARTIAL (u3/u4 fields and filters exist, but gate filtering violates derived truth, fn-0002); ac-0007 PARTIAL (u2 reads custom gate vocabulary initially, but version/schema refresh is absent, fn-0004); ac-0008 NOT MET (wave 0/u2/u3/u4, fn-0002 and fn-0003); ac-0009 PARTIAL (u2 uses one graph and preserves rels, but refreshed graph facts do not reach unchanged stored rows, fn-0004); ac-000a MET (u2 validate-only health); ac-000b MET for empty/non-error and recorded file-edge fixture (u2/u3/u4); ac-000c NOT MET (u2/root composition, fn-0004); ac-000d PARTIAL (u2/u4 absence and healthy-silent paths work, but live proof does not show a clean index built while missing, fn-0008); ac-000e MET (u1/u2; sweep_exclude indexes and is excluded only from doctor sweep); ac-000f MET (u1); ac-0010 MET (u1/u2); ac-0011 MET (all units; 9/9 gate observed); ac-0012 MET (u4); ac-0013 NOT MET as exit evidence and capability ownership (u4 fixture exists, but inbound document-edge query is not exposed and transcripts are absent, fn-0006/fn-0008).<br><br>EIGHT MEASURED DD SEMANTICS: CLEAN — no row-in-row recursion; any string-id object is a row and prefixes remain open metadata; no file#id short citation is emitted; prose fields come from schema declarations rather than names; no per-file ddocs links call exists on scan/watcher paths; health comes only from validate; not-applicable is excluded only for rel=pressure; sweep_exclude documents index normally. The final sweep-exclude test is meaningful rather than tautological: it parses the row, proves it remains present, and asserts the metadata bit; u1 independently proves discovery admission.<br><br>THREE NAMED PROOFS: reorder fixture and its sibling-position mutation are persisted; stored-vs-derived disagreement exists and was mutation-checked, but the surface filter fails that invariant (fn-0002); missing-binary and healthy-silent halves exist, with u2's repaired non-tautological oracle and named mutations persisted. The 7225-&gt;7225 count alone proves zero net new vector rows, not provider-call absence; combined with identical raw hashes and embed_items/get_smart_content early returns, the no-repeat embedding/summarizing call claim is supported.<br><br>ARCHITECTURE CLEAN: no new ddoc port trait; core remains pure; subprocess work stays in daemon; parser/store/daemon placement matches the crate doctrine; exactly one serde_json allowlist line and one forward migration; no trait-per-service, repository trait over sqlx, DTO mapping layer, domain/application/infrastructure folders, mocking framework, parser abstraction, or PG abstraction. Hit.ddoc is optional and skip-serialized. Schema facts demonstrably reach the parser on a healthy composition. Parsed edge and stored ref shapes agree. PARSER_VERSION changed once. Missing edge kind defaults to document. Empty rows_referencing is non-erroring.<br><br>RISKS: 1 retired; 2 not retired (derived implementation is narrower than the claimed rule); 3 partially retired (one graph call, but refresh does not update unchanged rows); 4 retired; 5 not retired; 6 retired; 7 retired by persisted per-seat versions; 8 not retired because version does not key re-derivation; 9 retired for embedding spend; 10 retired by committed fixtures; 11 not retired because the existing blob-keyed tree identity fails on duplicate-content paths. ac-000d's cwd-only notice is honestly narrowed: multi-root absence is not attributable. DL-008 is also worded honestly and severely, but remains ship-blocking: parser/version changes do not enqueue existing bytes. |

<a id="findings"></a>

## Findings

| id | kind | severity | finding | address | state | note | pressure | proven_by | links | receipt |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| fn-0001 | defect | MAJOR | Two valid files with identical bytes create two file roots under one (blob, parser_version), and every get for that content fails as corrupt. Ddoc citations make the inherited identity defect materially likely; search also resolves a shared raw_hash to one arbitrary element owner, collapsing distinct rows with identical prose/metadata-excluded text. | crates/store/src/elements.rs:115-161; crates/store/src/embeddings.rs:529-545 | [ ] unchecked | Isolated branch-built repro: byte-identical a.dd.json and nested/b.dd.json were indexed in one root; both flowspace3 get calls returned FS3-E-STORE-QUERY-FAILED: blob 8f1e5a83... has 2 file roots, expected exactly one. PM independently reproduced the same failure for ddocs and ordinary Rust. This is pre-existing and does not require a parallel ddoc table, but it prevents ac-0003 from being universally true and is ship-blocking until separately owned. | not-applicable | — | — | target/debug/flowspace3 get a.dd.json#acceptance_criteria/ac-a1b2 and nested/b.dd.json#acceptance_criteria/ac-a1b2 -&gt; identical store-corrupt errors after queue drained |
| fn-0002 | defect | MAJOR | --gate-open/--gate-closed filter only the stored-state gate_terminal bit and ignore state_derived, so the stored claim wins the plan's headline query when the two disagree. DdocMeta::effective_state() is not used by the query layer. | crates/store/src/embeddings.rs:475-477,540-542,641-644 | [ ] unchecked | Fresh isolated fixture returned state_stored=checked, gate_terminal=true, state_derived.complete=false with incomplete=[dw-d4e5]. --gate-closed returned the task; --gate-open excluded it. PM independently reproduced and confirmed this must be fixed by preferring derived.complete and falling back to stored only when derived is absent. | not-applicable | — | — | isolated target/debug/flowspace3 search contradictory completion --id-kind tk --gate-closed returned tk-b2c3; --gate-open returned no eligible content |
| fn-0003 | defect | MINOR | Derived-state enrichment is hardcoded to builder/plan done_when groups. Rows with valid derives edges to other sections or linked documents, including phase -&gt; tasks, always return state_derived=null. | crates/daemon/src/ddoc.rs:297-320 | [ ] unchecked | Live isolated search showed phase rows carrying rel=derives targets ending in #tasks while state_derived remained null. PM accepted this as a documented limitation pending dd's derive verb rather than duplicating recursive rollup logic. The limitation must be explicit anywhere derived-state coverage is claimed; ac-0008 is otherwise overbroad. | not-applicable | — | — | target/debug/flowspace3 search 'Contracts wave 0 PM authored' --id-kind ph showed derives edges with state_derived null |
| fn-0004 | defect | MAJOR | The ddocs version is never stamped into persisted row/graph metadata and does not key re-derivation. A new probe updates only an in-memory snapshot; unchanged blobs are not enqueued, and even a queued known blob returns before re-enrichment. Schema, graph, gate, validation, or ddocs-version changes therefore leave stored row facts stale indefinitely. | crates/core/src/ddoc_envelope.rs:15-20; crates/daemon/src/roots.rs:182-215; crates/daemon/src/scan.rs:142-146 | [ ] unchecked | The frozen u2 contract requires DdocGraph.version and ac-000c requires version-keyed clean re-derivation, but DdocGraph and DdocMeta contain no version. DdocTooling.version is ephemeral. DL-008 honestly records the parser-version enqueue defect, but the same path also defeats the new ddocs-version contract and the comments claiming the next add/rescan refreshes facts. | not-applicable | — | — | source trace: probe set before known-file loop; unchanged files skip enqueue; scan get_elements(Some) returns directly to enqueue_for_tree; no persisted ddocs version field exists |
| fn-0005 | defect | MAJOR | Fallback embedding is not observable to an agent. DdocMeta stores embed_basis, but DdocHit and GetResult drop it; schema-list or individual schema resolution failures quietly produce fallback rows while is_absent remains false because only version is checked. | crates/daemon/src/ddoc.rs:67-76,102-107; crates/daemon/src/search.rs:135-169 | [ ] unchecked | This violates ac-0005 and impl-guide risk r5. Isolated hit JSON contained no embed_basis. A healthy binary plus failed schema_names uses unwrap_or_default, suppresses all facts, emits fallback text, and omits the missing-binary notice. Surface embed_basis (and preferably schema-resolution degradation) and test the partial-tooling path. | not-applicable | — | — | isolated row hit JSON exposed schema/state/rels/findings but no embed_basis; code projection omits the field |
| fn-0006 | defect | MAJOR | The second highest-value query, 'which tasks claim this criterion', has no relation traversal surface. Link fields are deliberately absent from embedded row text, search has no rel/target predicate, and refs stores only kind=file edges. The eval scenario can therefore pass only by incidental semantic similarity, not by the satisfies edge it claims to answer. | crates/parsers/src/ddoc.rs:279-302; crates/daemon/src/search.rs:45-78,567-572; crates/store/src/ddoc.rs:167-210 | [ ] unchecked | This repeats the capability-ownership hole the PM found for source-file refs. eval/ddocs/tasks-claiming-criterion declares a ground truth address, while crates/cli/tests/ddoc_eval.rs only validates fixture shape and never executes the relation query. Add an exact document-edge traversal/query surface or narrow the product claim and AC/eval wording. | not-applicable | — | — | full product-code trace found no inbound document-edge API or relation-target SearchFilter |
| fn-0007 | defect | MAJOR | Every ddoc row and structural section container receives the whole document span. The summary policy uses span line count, so tiny rows and empty containers cross summary_min_lines and trigger avoidable summarizer work; empty containers also earn raw embeddings and remain searchable content. | crates/parsers/src/ddoc.rs:78,135-188,222-225; crates/daemon/src/scan.rs:123-125 | [ ] unchecked | Isolated output showed a five-line row snippet with span [1,15] and a smart summary; repo phase rows carry span [1,229]. Under real providers this converts a row parser into one paid summary per small row and can produce meaningless structural hits. Give rows truthful spans or base summary eligibility on raw_text; structural empty containers must not earn enrichment. | not-applicable | — | — | isolated target/debug search returned smart summary for a short row solely because the row span covered the 15-line file |
| fn-0008 | defect | MINOR | The persisted integration proof does not contain the promised transcripts for 'which ACs are open' or 'which tasks claim this criterion', and its missing-binary excerpt proves rows were served while the binary was absent, not that a clean corpus was indexed while absent. | docs/plans/008-ddocs-scan/assets/integration-proof.md:11-22,82-100; docs/plans/008-ddocs-scan/assets/tasks/phase-1/tasks.dd.json:378-399 | [ ] unchecked | The code and unit tests provide supporting evidence, but dw-002b/dw-002e require live end-to-end evidence and remain unchecked. Persist exact commands and complete result envelopes from an isolated zero-queue run before closing ac-0001/ac-000d/ac-0013. | not-applicable | — | — | exact search of integration-proof.md found none of the first two named query strings |
| fn-0009 | defect | MINOR | Dependency-state evidence contradicts itself: the plan/impl guide and eval resolution say dd PR #12 is open and file edges do not exist, while the refreshed task receipt says a real kind=file graph was recorded from dd main after PR #12 merged at 5aa18b7. | docs/plans/008-ddocs-scan/impl-guide.dd.md:101-105; docs/plans/008-ddocs-scan/assets/tasks/phase-1/tasks.dd.json:258-263 | [ ] unchecked | The current corpus may still correctly return an empty inverse result, but 'PR unmerged' is no longer a valid reason if the receipt is current. Reconcile the dated upstream state and preserve the stronger, corpus-specific empty proof. | not-applicable | — | — | repository artifacts contain both mutually exclusive upstream states |
| fn-000a | defect | MINOR | The wave-0 frozen contract says Element.ddoc is Option&lt;DdocMeta&gt;, while the composed public type is Option&lt;Box&lt;DdocMeta&gt;&gt;. Boxing is a sensible allocation decision and the task ledger describes it, but the authoritative frozen contract was never amended, so reviewers cannot distinguish an approved ruling from silent seam drift. | docs/plans/008-ddocs-scan/impl-guide.dd.md:35-45; crates/core/src/element.rs:220-231 | [ ] unchecked | Persist the contract amendment/rationale or restore the declared type. The implementation itself is architecturally sound; the finding is contract traceability under TENET 2. | not-applicable | — | — | direct comparison of frozen contract and composed public struct |
