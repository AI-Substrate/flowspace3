{
  "dd": {
    "schema": "pij-team/packet",
    "version": 1
  },
  "sections": [
    {
      "name": "meta",
      "value": {
        "title": "Coder packet — u-t the tui verb (007-human-interface)",
        "role": "coder",
        "plan": "docs/plans/007-human-interface/plan.dd.json",
        "seat": "(filled at canary)",
        "updated": "2026-08-28"
      }
    },
    {
      "name": "mission",
      "value": "You build ONE unit: `flowspace3 tui` — a modest, genuinely gorgeous ratatui dashboard that a person runs over ssh in a plain terminal and immediately understands: what is indexed, what the daemon is doing RIGHT NOW, and an answer to a question they type. Jordan accepted the POC verdict — ship a modest tui verb, defer React — so this is a real product surface, not a demo. You report to the PM (pij-near-carp), not to prime."
    },
    {
      "name": "instructions",
      "value": [
        {
          "id": "i1",
          "text": "Orient BEFORE touching anything: your unit row in /Users/jordanknight/substrate/flowspace/fs3-human-interface/docs/plans/007-human-interface/impl-guide.dd.md (BINDING, read its two AMENDED notes), plan.dd.md, assets/inputs/README.md (every input pinned by sha256), .agents/skills/pij-team/TENETS.md, and the frozen seams named in your interface section. Run `harness boot --json` in YOUR worktree first; its compose stage reports a FALSE degraded inside a worktree (docker compose ps is cwd-scoped; the shared db really is up on :5433) — that one stage is known and non-blocking, ruled by prime 2026-08-28. Never run `docker compose up`."
        },
        {
          "id": "i2",
          "text": "ACK FIRST, code second. Reply to the PM (pij-near-carp) with your read of the unit and a NUMBERED implementation plan, and state your worktree root in the ack. Corrections to this packet are welcome and outrank it — bring evidence (a source read, a command and its output). No code before the PM rules your ack by number."
        },
        {
          "id": "i3",
          "text": "DOGFOOD flowspace3: for any meaning-shaped question (\"where is X handled\", \"what owns Y\") run `flowspace3 search \"<question>\"` BEFORE grep — exact identifiers may still grep. The daemon is live on :7373 and this repo is indexed. Report every miss, bad next_action or confusing envelope to the PM as friction, and capture it with `harness observe` the moment it bites. List, NEVER clear, the observation buffer — it is shared across every agent in this tree."
        },
        {
          "id": "i4",
          "text": "ABSOLUTE PATHS FOR EVERY READ AND EDIT, ALWAYS (DL-007/008, proven with three seats on 2026-08-28): a spawned seat's file tools may resolve a relative path against the SPAWN directory while your shell resolves it against your worktree — the same relative path then names two different files, every tool reports success, and your gate goes green against a tree that does not contain your change. Prefix every path with your worktree root."
        },
        {
          "id": "i5",
          "text": "Environment discipline: export PIJ_SESSION_ID in your shell; CARGO_INCREMENTAL=0 on every cargo invocation; tests that need Postgres use the shared instance via FS3_TEST_DATABASE_URL (never start or stop a container); a rustc/LLVM \"IO failure\" is a FULL DISK, not a code bug — report `df -h` and stop. Put the Avail figure and the observed toolchain version (`cargo --version`) in every report you send."
        },
        {
          "id": "i6",
          "text": "Commit with `harness commit \"<conventional subject>\" -- <absolute paths>` as you go; push your branch. Status card at the start and end of each unit of work (`pij report now \"<did>\" \"<next>\"`, 280 chars max per field). NEVER merge, never open a PR, never touch another unit's paths — the PM composes."
        },
        {
          "id": "i7",
          "text": "Done = `harness checks` green in YOUR worktree AND tree-resident proof that what you claim is in the tree you built (git status/mtime for your files, the lockfile carrying your dependency, the test binary naming your module). An exit code is honest about what it built and silent about what it did not."
        },
        {
          "id": "i8",
          "text": "FLEET SAFETY — BINDING (prime, 2026-08-28, after DL-004: two seats bought 150 summaries and 2,475 vectors in fifteen minutes by booting test daemons against flowspace3_test with ambient Azure credentials in scope). If you hand-boot a daemon for ANY reason: mint a per-seat database (create it, use it, DROP it), point FS3_CONFIG_DIR at an EMPTY directory so the FAKE providers apply, bind a unique port, keep logs in your tree — and VERIFY the boot line prints embedder=fake summarizer=fake BEFORE you let it index anything. NEVER boot a daemon against flowspace3_test or the shipped default database. Cost signal to internalise: a fast summarize is a cache hit, a SLOW one is a live provider spending real money — if calls are slow, stop and check what you are pointed at. Full recipe: .harness/government/briefs/w-daemon-sandbox.md on main."
        },
        {
          "id": "u1",
          "text": "Read /Users/jordanknight/substrate/flowspace/fs3-human-interface/docs/plans/007-human-interface/assets/inputs/tui-poc-VERDICT.md first: it is the seat-written verdict from the ratatui POC (pij-mammoth-ox), including what was easy, what was PAINFUL, and the pain list — truncation and input handling — which is your polish checklist. The POC's code is scratch-only reference and is NOT in this tree: product code is fresh in-crate, one module, tested."
        },
        {
          "id": "u2",
          "text": "PANES, per the accepted verdict: roots (what is indexed, with file counts), operations/queue (depth by kind and state, with history), ACTIVITY (the real event feed — no mock tag anywhere, ever), and search that becomes results-dominant when focused. Keyboard: type + Enter to search, Tab cycles panes, / jumps to search, arrows select, Esc leaves search, q quits."
        },
        {
          "id": "u3",
          "text": "DATA, two sources and no third: the CLI's own JSON surface for snapshots (fs3_cli::DaemonClient — call it directly, do not shell out to the binary the way the POC did) and u-w's NDJSON event stream for activity (fs3_core::events types, GET /events). u-w is being built in parallel and you must NOT wait for it: build against a RECORDED NDJSON fixture committed under your tests, and keep the source behind one small seam so the PM's composition step is repointing it at the live stream — that swap is the named composition risk, so make it a one-line change and say where it is."
        },
        {
          "id": "u4",
          "text": "FORWARD COMPATIBILITY IS YOURS TO HONOUR: an event kind this build has never seen parses as fs3_core::events::EventKind::Unknown. Skip it and keep the stream — never error, never exit. A shipped TUI meeting a newer daemon is the normal case, not an edge case."
        },
        {
          "id": "u5",
          "text": "DEGRADE HONESTLY when the daemon is down: say so on screen in words a person can act on, keep the UI responsive, retry on a stated interval, and recover when it returns. A TUI that hangs or shows stale numbers as if they were live is worse than one that says the daemon is not answering. Same for a dropped stream: notice the heartbeat gap and reconnect."
        },
        {
          "id": "u6",
          "text": "ALLOWLIST GRANT, exact: ratatui and crossterm for fs3-cli ONLY, added to the arch allowlist in the same commit as the dependency. Nothing else is granted — no async runtime beyond the tokio already present, no widget zoo. The POC ran on one synchronous draw loop plus worker threads over a channel and started instantly in about 700 lines; that is the bar for both simplicity and speed."
        },
        {
          "id": "u7",
          "text": "YOUR ONLY EDITS OUTSIDE YOUR MODULE: one `Tui` subcommand arm in crates/cli/src/main.rs, one `pub mod tui;` line, and your two rows in crates/cli/Cargo.toml. Two other coders are in this crate right now; that is the entire collision surface and it is deliberate. Also: `tui` prints no envelope, so it must not disturb emit() — and `cargo test -p fs3-cli --test envelope_goldens` must stay green with the goldens unmodified."
        },
        {
          "id": "u8",
          "text": "TEST HEADLESS: layout and state logic unit-tested without a terminal (what the POC could not do and paid for); stream consumption tested against your recorded fixture including an unknown kind and a mid-stream disconnect; and a manual transcript at first light. `harness checks` must be green in your worktree without a tty present, so nothing may require one to compile or test."
        }
      ]
    },
    {
      "name": "working_with",
      "value": [
        {
          "id": "w1",
          "text": "The PM (pij-near-carp) is your only interface: acks, questions, done reports. Do not message prime or Jordan directly unless the PM's seat is dead."
        },
        {
          "id": "w2",
          "text": "DEFER to the PM: any change to the FROZEN seams (fs3_core::output, fs3_cli::render's signature, fs3_core::events wire types), any new dependency or allowlist row, anything outside your fence, and ANY diff to the byte-goldens."
        },
        {
          "id": "w3",
          "text": "Your measurement outranks this packet. If reality contradicts the brief, stop and say so with the evidence — do not silently code around it. One question per message."
        },
        {
          "id": "w4",
          "text": "THE INVARIANT ABOVE ALL: the JSON envelope on stdout is the product's contract with every agent and every `| jq` in the wild. Rendering is presentation applied AFTER envelope production. If `cargo test -p fs3-cli --test envelope_goldens` goes red, you have changed the agent contract — STOP and report; a golden is never re-captured to make a test pass."
        }
      ]
    },
    {
      "name": "scope",
      "value": {
        "worktree": "/Users/jordanknight/substrate/flowspace/fs3-hi-u-t",
        "branch": "007-u-t",
        "fence": "/Users/jordanknight/substrate/flowspace/fs3-hi-u-t: crates/cli/src/tui/** (your module, fresh product code); crates/cli/tests/tui*.rs plus your recorded NDJSON fixture; one Tui arm and one `pub mod tui;` line in crates/cli/src/main.rs; two dependency rows in crates/cli/Cargo.toml and the matching allowlist rows; docs/services/tui.md (your service page).",
        "forbidden_note": "Never write .harness/government/**, another seat's worktree, the pij-team skill folder, the plan's flow state, or the frozen seam files (crates/core/src/output.rs, crates/core/src/events.rs, crates/cli/tests/goldens/**). Never merge, never open a PR. SHARED-FILE MAP, engineered down to the minimum and binding on all three units: in crates/cli/src/main.rs — u-r touches ONLY the add/scan arms and the render wiring, u-w touches ONLY the Status arm plus one `pub mod watch;` line, u-t adds ONLY one `Tui` subcommand arm plus one `pub mod tui;` line. In crates/cli/Cargo.toml each unit adds only its own dependency rows. Nobody edits another unit's region, and any need to is a stop-and-ask."
      }
    },
    {
      "name": "done_bar",
      "value": [
        {
          "id": "d1",
          "text": "`flowspace3 tui` runs in a plain terminal over ssh and shows roots, queue with history, real activity, and in-pane search that goes results-dominant when focused."
        },
        {
          "id": "d2",
          "text": "The activity pane is driven by real events from the frozen wire — a recorded fixture until composition, the live stream after — and carries no mock tag anywhere."
        },
        {
          "id": "d3",
          "text": "Daemon down and stream dropped are both handled visibly and recoverably, proven by test, not by claim."
        },
        {
          "id": "d4",
          "text": "Layout and state logic are unit-tested headless; `harness checks` is green in your worktree with no tty, with tree-resident evidence, the observed toolchain version and the Avail figure."
        },
        {
          "id": "d5",
          "text": "The POC verdict's pain list (truncation, input handling) is addressed item by item, and your report says how each was handled."
        },
        {
          "id": "d6",
          "text": "docs/services/tui.md carries the SNAP-IN RECIPE: the exact arm and module lines, the allowlist rows, and — named explicitly — the ONE place the fixture source is swapped for the live stream."
        },
        {
          "id": "d7",
          "text": "Done report to the PM with an explicit ASSUMPTIONS section, especially every assumption about the live stream you have never actually talked to."
        }
      ]
    },
    {
      "name": "refs",
      "value": [
        {
          "id": "r0",
          "path": "/Users/jordanknight/substrate/flowspace/fs3-human-interface/.agents/skills/pij-team/TENETS.md",
          "note": "doctrine — esp. tenets 2-6: fill contracts never widen them, ship the snap-in RECIPE not the integration, mechanical done"
        },
        {
          "id": "r1",
          "path": "/Users/jordanknight/substrate/flowspace/fs3-human-interface/docs/plans/007-human-interface/impl-guide.dd.md",
          "note": "BINDING — your unit row, the composition contract, and the two AMENDED notes"
        },
        {
          "id": "r2",
          "path": "/Users/jordanknight/substrate/flowspace/fs3-human-interface/docs/plans/007-human-interface/assets/inputs/README.md",
          "note": "every plan input pinned by sha256, and what the POCs already proved so you cite instead of re-deriving"
        },
        {
          "id": "r3",
          "path": "/Users/jordanknight/substrate/flowspace/fs3-human-interface/AGENTS.md",
          "note": "dogfooding rules and the harness commit contract (CLAUDE.md is a symlink to this)"
        },
        {
          "id": "r4",
          "path": "/Users/jordanknight/substrate/flowspace/fs3-human-interface/docs/plans/007-human-interface/assets/inputs/tui-poc-VERDICT.md",
          "note": "the accepted verdict and your polish checklist (sha-pinned in assets/inputs/README.md)"
        },
        {
          "id": "r5",
          "path": "/Users/jordanknight/substrate/flowspace/fs3-human-interface/docs/services/event-stream.md",
          "note": "the wire you consume, including what each consumer needs"
        },
        {
          "id": "r6",
          "path": "/Users/jordanknight/substrate/flowspace/fs3-human-interface/crates/core/src/events.rs",
          "note": "the frozen types, including EventKind::Unknown — your forward-compat guarantee"
        }
      ]
    }
  ]
}
