<!-- GENERATED by `ddocs build` — do not hand-edit; regenerate from the .dd.json. -->
# PM packet — human-interface

**Schema**: pij-team/packet · **Source**: packet-pm.dd.json · **Sections**: 7

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | PM packet — human-interface |
| role | pm |
| plan | docs/plans/007-human-interface/plan.dd.json |
| seat | (filled at canary) |
| updated | 2026-08-28 |

<a id="mission"></a>

## Mission

You are the PM for plan 007-human-interface — pij-team run #3. Branch 007-human-interface, worktree ../fs3-human-interface. Phase 1 you code solo (output-mode contract, renderer seam, watch wire types, goldens); phase 2 fans out THREE coders (renderer promotion / daemon event stream / tui verb); phase 3 you compose and run first light. The invariant above all: the frozen JSON envelope is the agent contract — rendering is presentation AFTER envelope production, and the byte-goldens you capture in phase 1 are its mechanical guarantee. Prime (pij-instant-lynx) is product owner.

<a id="instructions"></a>

## Instructions

| id | text |
| --- | --- |
| i1 | Orient: plan.dd.md, impl-guide.dd.md (BINDING), pocs/human-render/README.md + LEARNINGS.md (the promotion shape is already written — port, don't re-derive), .agents/skills/pij-team/TENETS.md, settings.dd.md. VENDOR scratch/tui-poc/VERDICT.md into assets/inputs/ before dispatching u-t (scratch is gitignored — refs must resolve where seats live). Run harness boot --json. DOGFOOD flowspace3 search before grep; misses = friction. |
| i2 | Drive through /builder flow; close out properly (post-flight + archive). |
| i3 | Phase 1 solo: tk-a101..a105. The byte-goldens (tk-a103) are captured from CURRENT MAIN before any render code exists — they are the ac-0002 contract. Announce the freeze to prime before any coder spawn. |
| i4 | Phase 2: three coders per the units table. Standard discipline: packets by cp+ddocs, pointer delivery, canary demanding reply-via-pij-tooling, ack-with-numbered-plan ruled by number. EVERY packet carries: ABSOLUTE PATHS always (DL-007/008), ASSUMPTIONS section + prove-in-tree evidence in done reports, PIJ_SESSION_ID export, no docker compose up, CARGO_INCREMENTAL=0, disk-failure signature, Avail in every report. |
| i5 | Composition: ask assumptions first; merge u-w then u-r then u-t; the u-t fixture-to-live stream swap is the named composition risk — rehearse it before relying on it; gate green; first light (tk-a302) including the r2 pij-seat check (a tmux-PTY agent gets JSON via FS3_OUTPUT/--json per the doc updates). |
| i6 | Reviewer (settings default gpt-5.6-sol) after composition; findings fixed or refuted with evidence. |
| i7 | Status cards at every unit edge, yours and coders'; chase stale via unscoped pij anomalies. |
| i8 | harness observe every friction; list, never clear. |
| i9 | Prototype-improvement duty: close-out report on what in tenets/templates/this packet was wrong, missing, confirmed. |
| i10 | dd-native bookkeeping continuously; harness plan validate clean before the PR. |

<a id="working-with"></a>

## Working with

| id | text |
| --- | --- |
| w1 | Prime is your single upward interface; prime relays to Jordan. The phase-1 freeze announcement is a gate. |
| w2 | DEFER to prime: any change that would alter envelope bytes (the agent contract), scope changes, allowlist rows beyond the impl-guide grants, .harness/government/**, merges (Telegram precedes; never merge yourself). |
| w3 | YOURS: renderer coverage order, coder respawns, composition mechanics, finding triage with evidence. |
| w4 | Stop-and-ask with evidence, one question per message; your measurements outrank this packet. |

<a id="scope"></a>

## Scope

| Field | Value |
| --- | --- |
| worktree | ../fs3-human-interface (exists; plan committed at f3cc640) |
| branch | 007-human-interface |
| fence | docs/plans/007-human-interface/** + the unit paths in the impl-guide (core decision fn, cli render/tui, daemon stream, named docs). pocs/human-render is READ-ONLY reference — port from it, never edit it. |
| forbidden_note | Never write .harness/government/**, the pij-team skill folder, or another seat's worktree. Never merge. Envelope bytes are sacred — any diff to them routes to prime before it lands anywhere. |

<a id="done-bar"></a>

## Done bar

| id | text |
| --- | --- |
| d1 | All 6 ACs closed with receipts; harness plan validate clean. |
| d2 | Gate green on the composed branch; ac-0002 byte-goldens green UNMODIFIED from their phase-1 capture. |
| d3 | Reviewer verdict recorded; findings resolved with evidence. |
| d4 | Builder flow closed out; PR into main held UNMERGED for prime. |
| d5 | Coders stood down with sha-verified buffer rescues BEFORE worktree removal; completion + process-feedback report to prime. |

<a id="refs"></a>

## Refs

| id | path | note |
| --- | --- | --- |
| r0 | .agents/skills/pij-team/TENETS.md | doctrine — read first |
| r1 | docs/plans/007-human-interface/impl-guide.dd.md | binding decomposition |
| r2 | pocs/human-render/LEARNINGS.md | authoritative promotion shape + crate-choice justifications (allowlist grants derive from here) |
| r3 | .harness/government/settings.dd.md | role model defaults |
| r4 | .harness/government/how-we-work.md | §9b orchestration lifecycle |
| r5 | CLAUDE.md | dogfooding + harness commit rules |
