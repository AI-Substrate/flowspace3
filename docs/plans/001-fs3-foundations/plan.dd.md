<!-- GENERATED by `ddocs build` — do not hand-edit; regenerate from the .dd.json. -->
# fs3 Foundations

**Schema**: builder/plan · **Source**: plan.dd.json · **Sections**: 20

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | fs3 Foundations |
| slug | fs3-foundations |
| ordinal | 1 |
| status | ready |
| complexity | CS-3 |
| summary | Build the fs3 mold: workspace, DI architecture, drift enforcement, exemplars. |
| backpressure | — |
| log | — |
| mode | Simple |
| plan_version | 1.0.0 |
| created | 2026-08-26 |
| confidence | 0.85 |
| complexity_breakdown | S=1 I=1 D=1 N=1 F=1 T=2 (sum 7 -&gt; CS-3) |

<a id="summary"></a>

## Summary

Plan 001 does not build fs3's features - it builds the mold the rest of fs3 is poured into: the cargo workspace implementing workshop 001's architecture (7 crates: core, parsers, providers, store, testkit, daemon, cli), full DI via exactly two trait ports (Embedder, Summarizer) wired at one composition root with config-driven provider selection including 'fake', a dockerized Postgres+pgvector compose stack, mechanical architecture-drift enforcement wired into harness checks, and one exemplar of every artifact kind later plans copy (pure-core unit test, port contract test, PG integration test, parser fixture test, build/check pipeline, docs shape). Exit bar: a fresh agent can implement any later feature by copying shapes already present, and architecture violations go red mechanically before review. Shipping this plan proves workshop 001's promotion-gate conditions 1-3; condition 4 (contract tests green over at least one REAL provider) needs an on-demand keyed run - the promotion move to docs/rules-idioms-architecture/ executes only once that run is recorded, else it is a named follow-up.

<a id="goals"></a>

## Goals

- Cargo workspace with the 7-crate layout from workshop 001, compiling end-to-end at hello-world depth.
- DI/IoC in the flesh: two trait ports, one composition root, config-driven provider selection including fake.
- Architecture drift detected mechanically: crate-dependency allow-list check + clippy/fmt wired into harness checks.
- Exemplar tests at every tier later plans copy: core unit (no doubles), port contract, PG integration, parser fixture.
- Docker compose stack (Postgres+pgvector) and config loading from ~/.config/flowspace3.
- README quick-start + docs/how/architecture.md signposted from README; harness boot healthy.

<a id="non-goals"></a>

## Non-goals

- The real indexing pipeline (queue, watcher, scan) - plans 002+.
- Search (semantic/text/regex), summaries, embeddings as real flows - later plans.
- Conversations, MCP server, daemon web UI - later plans.
- Real provider network calls as a gate requirement (fake providers suffice; real impls are compile-proven stubs).
- The full tree-sitter grammar pack (parsers crate carries rust+markdown as the exemplar only).
- flowspace3 doctor's full stack-repair flow (a ping/health exemplar stands in; doctor is a later plan).
- Config hot-reload and per-worktree overrides (config loading lands; reload/override mechanics later).
- Daemon containerization (PRD req 33's daemon-in-compose) - deferred: phase 1 runs the daemon on the host against composed PG; a later plan moves it into the compose stack.

<a id="acceptance-criteria"></a>

## Acceptance criteria

| id | claim | state | note | receipt | pressure | proven_by |
| --- | --- | --- | --- | --- | --- | --- |
| ac-0001 | From a clean checkout, the documented steps yield green `cargo build --workspace` and `cargo test --workspace`. | [ ] unchecked | — | — | [bp-0001](assets/backpressure.dd.md#rows) | — |
| ac-0002 | The crate graph matches workshop 001 exactly; core compiles with no tokio/sqlx/HTTP dependencies. | [ ] unchecked | — | — | [bp-0002](assets/backpressure.dd.md#rows) | — |
| ac-0003 | The drift check FAILS when a forbidden dependency is added (negative test: add sqlx to core, check goes red, revert). | [ ] unchecked | — | — | [bp-0003](assets/backpressure.dd.md#rows) | — |
| ac-0004 | `docker compose up` brings Postgres+pgvector; the store integration test connects, applies migration 0001, and round-trips a row. | [ ] unchecked | — | — | [bp-0004](assets/backpressure.dd.md#rows) | — |
| ac-0005 | The daemon boots with fake providers selected via config from the flowspace3 config dir and serves GET /health on localhost; `flowspace3 ping` reports it healthy. | [ ] unchecked | — | — | [bp-0005](assets/backpressure.dd.md#rows) | — |
| ac-0006 | Exemplar tests pass at all tiers: core unit with zero doubles, Embedder contract over FakeEmbedder, PG integration; FakeEmbedder is deterministic (same text yields the same vector, asserted). | [ ] unchecked | — | — | [bp-0006](assets/backpressure.dd.md#rows) | — |
| ac-0007 | `harness checks` runs fmt --check, clippy -D warnings, tests, and the drift check, all green; `harness boot` reports healthy. | [ ] unchecked | — | — | [bp-0007](assets/backpressure.dd.md#rows) | — |
| ac-0008 | README carries the quick-start and signposts docs/how/architecture.md, which exists and reflects workshop 001. | [ ] unchecked | — | — | [bp-0008](assets/backpressure.dd.md#rows) | — |

<a id="phases"></a>

## Phases

| id | title | brief | state | note | receipt | depends_on | tasks | objective | delivers | key_risks |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ph-78fc | Phase 1: Foundations — workspace, DI architecture, drift enforcement, exemplars | The whole plan in one phase: scaffold the workspace, land the architecture as code, enforce it mechanically, and leave one exemplar of everything. Details in the task file. | [ ] unchecked | — | — | — | [tasks](assets/tasks/phase-1/tasks.dd.md#tasks) | — | — | — |

<a id="research-context"></a>

## Research Context

No research-dossier.md; richer equivalents consumed: fs2 prior-art survey; POC spike assets/poc/treesitter-results.md (reqs 2/3/22 PROVEN, 4,914 files/s, 11 learnings); workshop 001 (authoritative architecture); base-prd.md (43 requirements, all open questions resolved 2026-08-26).

<a id="testing-strategy"></a>

## Testing Strategy

| Field | Value |
| --- | --- |
| approach | Hybrid, exemplar-first (workshop 001 per-crate table) |
| rationale | This plan's tests ARE deliverables - one exemplar per tier that later plans copy. |
| focus_areas | core pure functions (no doubles); port contract over fakes; store/daemon integration vs real dockerized PG; parser fixture test. |
| excluded | Live network calls to real providers (real-impl contract tests are #[ignore]d, on demand). |
| mock_usage | No mocking frameworks ever; reusable fakes in testkit (workshop 001 rule 5); fake is a legal runtime provider config. |

<a id="documentation-strategy"></a>

## Documentation Strategy

| Field | Value |
| --- | --- |
| location | Hybrid: README.md quick-start + docs/how/ guides |
| rationale | Jordan 2026-08-26: docs/how and README, with a signpost to the architecture guide under docs/how. |

<a id="risks-assumptions"></a>

## Risks & Assumptions

Assumes docker + uv-era toolchain present on dev machines; assumes workshop 001 stands (any change re-opens this plan's drift rules); PG is Postgres 16 + pgvector via compose.

<a id="open-questions"></a>

## Open Questions

| question | state | note |
| --- | --- | --- |
| When to run the real-provider contract test (needs API keys) - it gates workshop 001's promotion (condition 4). | [ ] unchecked | Jordan's call; recorded by validate-v2 2026-08-26 |

<a id="workshop-opportunities"></a>

## Workshop Opportunities

| topic | type | why | key_questions | status |
| --- | --- | --- | --- | --- |
| fs3 architecture | Integration Pattern | Settle crate layout, ports, DI, testing before any code. | Covered: DI without a container; hexagonal in Rust; fakes over mocks; crate graph. | COMPLETE - assets/workshops/001-architecture.md (authoritative; this plan implements it) |
| PG schema for content+ref layers | Storage Design | Blob-keyed content vs ref layers deserves detailed design before plan 002 builds the pipeline. | Table shapes; (blob, model, prompt-version) keying; tag storage (PRD req 36); GC. | OPEN - deliberately deferred to plan 002 (this plan ships only migration 0001 as an exemplar) |

<a id="clarifications"></a>

## Clarifications

| session | question | answer |
| --- | --- | --- |
| 2026-08-26 | Workflow mode? | Simple, exactly one phase (Jordan: '/builder the plan, 1 phase please'). |
| 2026-08-26 | Testing strategy? | Answered by workshop 001 (authoritative): exemplar-first hybrid, per-crate table. |
| 2026-08-26 | Mock usage? | Answered by workshop 001: never mock; reusable fakes in testkit + fake as runtime provider. |
| 2026-08-26 | Documentation strategy? | Hybrid README + docs/how, architecture guide signposted from README (Jordan, this session). |
| 2026-08-26 | Branch and landing policy? | Jordan (ruling relayed by o-prime, brief amended in place): work DIRECTLY on main — no s001 stream branch; commit to main as you go; first commit is planning substrate excluding .claude/; landing bar = phase exit gates green, no PR ceremony. |

<a id="planning-seam"></a>

## Planning Seam

| artifact | present | effect |
| --- | --- | --- |
| assets/workshops/001-architecture.md | yes | authoritative design decisions - this plan implements them |
| base-prd.md | yes | business source, 43 requirements |
| assets/poc/treesitter-results.md | yes | feasibility proven; informs Key Findings |
| assets/research-dossier.md | no | equivalents above; no further research pass needed |
| OPEN workshop: PG schema design | deferred | deliberately deferred to plan 002; this plan ships only exemplar migration 0001 |

<a id="gate-matrix"></a>

## Gate Matrix

| gate | check | status | notes |
| --- | --- | --- | --- |
| G1 | Clarify | PASS | No [NEEDS CLARIFICATION] markers; all 4 Round-1 answers recorded. |
| G2 | Constitution | N/A | No docs/project-rules/constitution.md. |
| G3 | Architecture | PASS | No docs/project-rules/architecture.md yet; plan implements workshop 001, the authoritative architecture source; drift enforcement is itself a deliverable. |
| G4 | ADR Compliance | N/A | No docs/adr/. |
| G5 | Structure | PASS | All required sections present in dd-native form. |
| G6 | Testing Alignment | PASS | Hybrid strategy; every code task row carries its test in Done-When; exemplar tests are explicit tasks. |
| G7 | Domain Completeness | N/A | Domain mode off. |

<a id="implementation-summary"></a>

## Implementation Summary

One phase. Order: workspace scaffold -> core (types, pure fns, ports) -> testkit fakes -> providers stub -> parsers exemplar -> compose+store -> config -> daemon -> cli -> drift check -> harness extensions -> docs -> evidence sweep. Every task's Done-When is mechanical (a command that passes or a file that exists with stated properties).

<a id="key-findings"></a>

## Key Findings

| impact | finding | action |
| --- | --- | --- |
| Critical | POC: file discovery beats parser tuning (identical elements from 11% of bytes, 13.8x faster). | Foundations need no perf work; discovery filtering is a plan-002 concern (PRD req 41). |
| Critical | POC: fs2's name-pattern classifier invents elements (C++ 58 claimed vs 22 real). | core's classify exemplar ships the declaration-shape heuristic gate from day one (PRD req 42). |
| High | Harness is adopted but degraded: checks/boot expect Cargo.toml that doesn't exist yet. | This plan turns peacock's harness green - checks/boot extensions are updated as tasks, not later. |
| High | async fn in dyn-usable traits still needs #[async_trait] for object safety. | Both ports use #[async_trait]; recorded in the architecture guide. |
| Medium | POC: 20 grammar crates added 18.7s clean-build; foundations don't need them. | parsers carries rust+markdown only; grammar pack lands with the real pipeline. |
| Medium | sqlx compile-time query checks need a live DB or offline cache. | Use runtime queries (sqlx::query) in exemplars, or check in .sqlx cache; decide in-task, note in execution log. |

<a id="acceptance-coverage"></a>

## Acceptance Coverage Map

| ac | covered_by | verified_in |
| --- | --- | --- |
| ac-0001 | tk-0001, tk-0007, tk-000e | tk-000e quick-start walk |
| ac-0002 | tk-0001, tk-0002, tk-0003 | tk-000c drift check on real graph |
| ac-0003 | tk-000c | tk-000c negative test |
| ac-0004 | tk-0007, tk-0008 | tk-0008 integration test |
| ac-0005 | tk-0009, tk-000a, tk-000b | tk-000a/tk-000b tests |
| ac-0006 | tk-0002, tk-0004, tk-0008 | per-task exemplar tests incl. determinism assert |
| ac-0007 | tk-000c, tk-000d | harness checks + harness boot runs |
| ac-0008 | tk-000e | file existence + README link check |

<a id="risks"></a>

## Risks

| risk | likelihood | impact | mitigation |
| --- | --- | --- | --- |
| Docker unavailable when tests run (CI or fresh machine) | Medium | Medium | Integration tests detect missing docker and fail with the exact compose command; harness boot names it. |
| Drift check too weak (passes things workshop 001 forbids) | Medium | High | AC-03 negative test: deliberately violate, prove red, revert - the check's own exemplar. |
| Exemplars over-engineered, becoming the very ceremony rule 3 refuses | Medium | Medium | Each exemplar is the minimum that demonstrates the shape; review cites workshop 001's refused-list. |

<a id="execution-guardrails"></a>

## Execution Guardrails

- Workshop 001 is authoritative - do not contradict it; exactly two trait ports, a third is stop-and-ask.
- core must not depend on tokio/sqlx/reqwest/axum; the drift check enforces the whole crate graph.
- No mocking crates in any Cargo.toml; doubles come only from testkit.
- Never hand-edit plan.dd.md / tasks.dd.md (generated).
- Binary is flowspace3; config dir ~/.config/flowspace3 with FS3_CONFIG_DIR override for tests.
- fs3 writes nothing outside the workspace, docs, and compose files (PRD req 39 spirit applies to this repo too).
