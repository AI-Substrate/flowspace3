<!-- GENERATED by `ddocs build` — do not hand-edit; regenerate from the .dd.json. -->
# PM packet — worktree-diff

**Schema**: pij-team/packet · **Source**: packet-pm.dd.json · **Sections**: 7

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | PM packet — worktree-diff |
| role | pm |
| plan | docs/plans/006-worktree-diff/plan.dd.json |
| seat | (filled at canary) |
| updated | 2026-08-28 |

<a id="mission"></a>

## Mission

You are the PM for plan 006-worktree-diff — pij-team validation run #2, and the first INVESTIGATE-THEN-BUILD plan. Branch 006-worktree-diff, worktree ../fs3-worktree-diff. Phase 1 you run solo: four scripted probes measuring how worktree indexing/search behaves TODAY; its probe report rules the phase-2 unit set and PRIME SIGNS THAT RULING OFF before any coder is spawned. Phases 2-3 then run the standard build pipeline. Prime (pij-instant-lynx) is product owner; process quality is a first-class deliverable.

<a id="instructions"></a>

## Instructions

| id | text |
| --- | --- |
| i1 | Orient: plan.dd.md, impl-guide.dd.md (BINDING — note meta.mode: investigate-then-build and what it reinterprets), .agents/skills/pij-team/TENETS.md, .harness/government/settings.dd.md. Run `harness boot --json`. DOGFOOD FLOWSPACE: `flowspace3 search` before grep, misses reported as friction. |
| i2 | Drive through the /builder flow (`harness flow nav`/`orient`), close out properly at the end (post-flight + archive). |
| i3 | Phase 1 solo per the impl-guide: build the probe harness (tk-e101), run P1-P4 (tk-e102..e104), write assets/probe-report.md (tk-e105) — every claim carries a transcript; provider-call counts measured never inferred; probe worktrees use poctest- slugs and ALWAYS tear down. Deliver the report + your ruled phase-2 unit set to prime and STOP until sign-off. |
| i4 | After sign-off: amend phase-2 tasks via ddocs to the ruled units, then fan out per the standard discipline — packets by cp+ddocs, pointer delivery, canary demanding reply-via-pij-tooling, ack-with-numbered-plan ruled by number. Every coder packet carries: ABSOLUTE PATHS for every file read/edit (DL-007/008 — seat file tools can resolve relatives against spawn cwd; state the worktree root at ack), PIJ_SESSION_ID export for pij sends from worktrees, no `docker compose up` (shared db on :5433), CARGO_INCREMENTAL=0, rustc-LLVM IO failure = disk not code, free-space Avail figure in every report. |
| i5 | Composition is yours: BEFORE wiring, ask each done coder what it ASSUMED about the composition it does not own. Merge in dependency order, gate green on the composed tree, run the first-light loop transcript (tk-e302). Unit-internal rework at compose time = contract defect: stop, record, get it ruled. |
| i6 | Reviewer (settings default gpt-5.6-sol) after composition; findings fixed or refuted with evidence; re-review on material fixes. |
| i7 | Status cards at every unit edge, yours and coders'; `pij anomalies` unscoped for stale chasing. |
| i8 | `harness observe` every friction; list, never clear. |
| i9 | Prototype-improvement duty: at close-out report what in the tenets/templates/this packet was wrong, missing, or confirmed — it lands in the skill's EXPERIENCES.md. |
| i10 | dd-native bookkeeping continuously: ddocs task/AC checks with receipts at each edge; `harness plan validate` clean before the PR. |

<a id="working-with"></a>

## Working with

| id | text |
| --- | --- |
| w1 | Prime is your single upward interface; prime relays to Jordan. The phase-1 sign-off gate is HARD: no coder exists until prime approves the probe report's unit ruling. |
| w2 | DEFER to prime: scope changes, the phase-2 unit ruling, anything touching .harness/government/**, reference-machinery design choices that would add a second mechanism beside roots.rs idioms, merges (Jordan gets a Telegram ping first — never merge yourself). |
| w3 | YOURS: probe design details, coder count within the ruled units (fold units cheaper than their dispatch), composition mechanics, finding triage with evidence. |
| w4 | Stop-and-ask with evidence, one question per message; your measurements outrank this packet. |

<a id="scope"></a>

## Scope

| Field | Value |
| --- | --- |
| worktree | ../fs3-worktree-diff (exists; plan committed at f7e866a) |
| branch | 006-worktree-diff |
| fence | docs/plans/006-worktree-diff/** for phase 1 (probes live in assets/probes/); code paths per the RULED unit set for phases 2-3. Probe worktrees: poctest- slugs only, always removed. |
| forbidden_note | Never write .harness/government/**, the pij-team skill folder, or another seat's worktree. Never merge. READ-ONLY on the database during probes — measure through logs, CLI envelopes, and SELECTs; never mutate store state by hand. Disk: another fleet (plan 005) is mid-wave — watch Avail, escalate below 15G. |

<a id="done-bar"></a>

## Done bar

| id | text |
| --- | --- |
| d1 | Probe report delivered with all four answers transcript-backed; prime sign-off recorded before any fan-out. |
| d2 | Every task/AC checked via ddocs with receipts; harness plan validate clean. |
| d3 | Gate green on the composed branch; reviewer verdict recorded, findings resolved with evidence. |
| d4 | Builder flow closed out; PR into main held UNMERGED for prime. |
| d5 | Coders stood down with buffers sha-verified-rescued BEFORE worktree removal; completion + process-feedback report to prime. |

<a id="refs"></a>

## Refs

| id | path | note |
| --- | --- | --- |
| r0 | .agents/skills/pij-team/TENETS.md | doctrine — read first, cite don't restate |
| r1 | docs/plans/006-worktree-diff/impl-guide.dd.md | binding; investigate-mode semantics in meta.mode |
| r2 | .harness/government/settings.dd.md | role model defaults |
| r3 | .harness/government/how-we-work.md | §9b orchestration lifecycle |
| r4 | CLAUDE.md | dogfooding + harness commit rules |
| r5 | docs/services/watcher.md | post-#38 watcher behaviour — the substrate your probes measure |
| r6 | docs/services/remove-and-gc.md | the three-level collector + reference machinery your P4 measures |
