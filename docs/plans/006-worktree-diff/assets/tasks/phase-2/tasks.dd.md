<!-- GENERATED by `ddocs build` — do not hand-edit; regenerate from the .dd.json. -->
# Phase 2: Close the measured gaps (PROVISIONAL)

**Schema**: builder/plan · **Source**: tasks.dd.json · **Sections**: 4

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | Phase 2: Close the measured gaps (PROVISIONAL) |
| slug | phase-2 |
| ordinal | — |
| status | draft |
| complexity | — |
| summary | Placeholder unit set — REPLACED by the phase-1 probe report's ruled units before any coder is spawned. |
| backpressure | — |
| log | — |
| mode | — |
| plan_version | — |
| created | — |
| confidence | — |
| complexity_breakdown | — |

<a id="summary"></a>

## Summary

Do not dispatch from this file as-is: tk-e105 amends it from evidence, prime signs off, THEN fan-out.

<a id="tasks"></a>

## Tasks

| id | title | domain | phase | state | note | receipt | done | success | notes | satisfies |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| tk-e201 | u-a RULED — worktree lifecycle detector (absorbs create AND remove): a Reconcile implementor in crates/daemon/src/worktrees.rs diffs the live git worktrees of registered repos against the worktrees table, registering what appeared and unregistering what vanished, through the EXISTING roots.rs verbs. Reclamation stays gc's. Bound, per prime's rider: O(files) on FIRST registration only — an unchanged registered worktree must enqueue NOTHING on later passes, proven by a probe line. | — | — | [ ] unchecked | — | — | — | probe.sh flips p1_auto_discovered 0-&gt;1 and p4_root_still_registered 1-&gt;0 with no manual CLI call, p4_served_after_gc stays 0, and a new probe line shows 0 jobs enqueued for an unchanged worktree across repeated reconcile passes. | — | [ac-0002](../../../plan.dd.md#acceptance-criteria) |
| tk-e202 | PROVISIONAL u-b diff-scoped scanning: worktree scan walks against the main tree's blob set, spending nothing on identical content. | — | — | [x] na | RULED OUT by prime 2026-08-28 on probe evidence: P2a measured 305 identical-content files costing 0 elements, 0 summaries, 0 vectors and 0 provider round trips (enrich.rs:552-567 returns before the provider). The saving already exists; only the trigger was missing, and that is u-a. | Ruled out by prime 2026-08-28 on probe evidence (probe-report.md, run-20260828T024227Z P2a). | — | Ruled in/out by probe P2. | — | [ac-0002](../../../plan.dd.md#acceptance-criteria) |
| tk-e203 | u-c RULED — search honours the checkout it is asked from (crates/daemon/src/search.rs): (1) apply scope.worktree the way read.rs:619 already does, so a divergent path resolves to the asking checkout's version; (2) per-result provenance naming the checkout that served each hit; (3) truncation visibility — the default cap of 10 ALREADY exists at search.rs:141, so name the cap and what was truncated in the envelope rather than building a cap. | — | — | [x] checked | — | Initial unit eeb5b0e plus scoped representative correction: mutation test failed without the anchor and passed with it; store pg_first_light 18/18, daemon first_light 14/14, conversation_query 7/7; final isolated harness checks 9/9. PM owns composed p3_unresolved_rows probe. | — | probe.sh flips p3_search_context_sensitive 0-&gt;1 and p3_wrong_version_leak_to_main 1-&gt;0 while p3_get_context_sensitive stays 1; a hit that exists only in a non-caller checkout is excluded by default; the envelope names the cap when it bites and carries the weak-match hint in meta/hint (question-shape hint moved to w-agentic-query); assertions compare result identity or rank order, never raw scores (DL-002). | — | [ac-0003](../../../plan.dd.md#acceptance-criteria) |
| tk-e204 | PROVISIONAL u-d removal lifecycle: worktree remove -&gt; dereference -&gt; GC, no orphaned paid enrichment. | — | — | [x] na | ABSORBED into u-a (tk-e201) by prime's ruling 2026-08-28: creation and removal are one question with one owner — 'which live worktrees of registered repos exist?' — answered by one reconcile pass. P4 proved the removal MECHANISM is already clean (149 elements + 8 vectors reclaimed, orphaned-paid 0); only the detector was missing. | Absorbed into u-a (tk-e201) by prime's ruling 2026-08-28; P4 proved the removal mechanism already works. | — | Ruled in/out by probe P4. | — | [ac-0004](../../../plan.dd.md#acceptance-criteria) |

<a id="done-when"></a>

## Done when

_No fields._
