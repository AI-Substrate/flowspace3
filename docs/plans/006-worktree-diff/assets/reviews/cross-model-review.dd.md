<!-- GENERATED by `ddocs build` — do not hand-edit; regenerate from the .dd.json. -->
# Cross-model review — plan 006 worktree-diff

**Schema**: builder/review · **Source**: cross-model-review.dd.json · **Sections**: 3

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | Cross-model review — plan 006 worktree-diff |
| reviewer | pij-chubby-amphibian |
| model | github-copilot/gpt-5.6-sol high |
| round | 1 |
| date | 2026-08-28 |
| plan | [meta](../../plan.dd.md#meta) |
| note | APPROVE WITH NOTES after re-judgement at HEAD 365897f. Packet severities map to this schema as HIGH=MAJOR and MEDIUM=MINOR. Every review finding is fixed and independently re-judged. AC judgments: ac-0001 TRUE — the measured phase-1 report and run artifacts answer all four baseline questions; ac-0002 TRUE — isolated run 20260828T041857Z measured p1_auto_discovered=1 and p1_unchanged_reconcile_jobs=0, while phase-1 artifacts and unchanged enrichment guards support zero provider spend for identical content; ac-0003 TRUE at the deterministic code/contract layer — raw and smart representative selection now apply caller scope before LIMIT and at each chooser, with feature-scoped fixtures proving the caller-held hit is returned — while the plan's live semantic go-live receipt remains deliberately pending after merge; ac-0004 TRUE — WorktreeSupervisor delegates to daemon remove, and composed run 20260828T035625Z measured p4_root_still_registered=0, p4_deleted_content_still_served=0, p4_served_after_gc=0 with successful GC; ac-0005 FIXED IN TREE — 23a9b93 repairs the isolated-run abort exposed by the review, with the complete post-fix probe rerun remaining PM-owned; ac-0006 TRUE — harness checks exited successfully across nine gates and no dependency manifest/allowlist change exists. Composition seam: u-a makes many same-repository checkouts normal; that data shape exposed both raw and smart u-c chooser defects, now closed by de054e6 and c44b595. No second reference mechanism was introduced: worktrees.rs calls roots::add_root and remove::remove; GC remains separate. Search admission is inside the nearest-neighbour CTE before LIMIT; raw element and smart-content choices repeat caller scope; smart tie ordering is total (`created_at, model_key, raw_hash`); truncation is measured with limit+1; weak-match remains advisory only. Mutation sensitivity: the false→Err→false test now proves ambiguity resets the absence streak; the raw two-blob fixture proves scoped representative choice; the smart-source fixture maps one text_hash to foreign and caller raw_hashes and asserts exactly one caller-held smart hit IS RETURNED with caller root/path/blob; the daemon suite asserts result identity rather than numeric score. Fresh re-judgement: daemon worktrees 11/11, store pg_first_light 19/19, daemon first_light 14/14. Revised verdict is APPROVE WITH NOTES solely because the complete post-fix probe and live ac-0003 go-live receipt remain external PM gates, not because any review finding remains open. |

<a id="findings"></a>

## Findings

| id | kind | severity | finding | address | state | note | pressure | proven_by | links | receipt |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| f-001 | defect | MAJOR | HIGH — scoped search admits a caller-held raw hash before LIMIT, then independently chooses the globally lowest-id element carrying that hash. When identical element text appears in different file blobs, the chosen representative can belong to another checkout; the scoped provenance LEFT JOIN resolves nothing, yet the row is still returned with path, repo, and worktree null and an address stripped to bare el:&lt;path&gt;. Claim violated: every scoped hit belongs to and names the asking checkout. Proof: the isolated same-repository corpus contained 227 searchable raw hashes with a foreign lowest-id representative; from cwd fs3-006-uc, query `mistyped override value expected integer --source raw --limit 10` returned `coerce` as `el:crates/core/src/config.rs::coerce` with all provenance null. Smallest fix: re-apply repo/path/worktree/kind scope inside every LATERAL chooser that selects smart_content or an element, and require the final representative to resolve through the scoped anchor. Add a mutation-sensitive fixture where the same raw_text/raw_hash occurs in two different file blobs, main is indexed first, and a feature-scoped query must choose the later feature-held representative with non-null provenance. | /Users/jordanknight/substrate/flowspace/fs3-worktree-diff/crates/store/src/embeddings.rs:466-511 | — | — | — | — | — | docs/plans/006-worktree-diff/assets/reviews/runtime/uc-resolver-mismatch.json; SQL exposure count=227; existing pg_first_light 17/17 and daemon first_light 14/14 still passed, proving the gap in current tests |
| f-002 | defect | MAJOR | HIGH — the committed regression probe is not runnable end-to-end under the prescribed isolated fake-provider setup. With `set -e`, the first EMBEDDER command substitution exits 1 when daemon-p1.log lacks the boot line; Bash terminates before the intended full-log fallback, before fake-provider refusal fields, the resolved-row invariant, exposure census, and all P4 checks. Claim violated: ac-0005's one-command future-seat regression harness and the review packet's predicate-flip done bar. Smallest fix: make both provider-marker lookups explicitly non-fatal (`... \|\| true`) and let the existing empty/unknown branch decide; re-run through DONE and retain the receipt. | /Users/jordanknight/substrate/flowspace/fs3-worktree-diff/docs/plans/006-worktree-diff/assets/probes/probe.sh:437-438 | — | — | — | — | — | fresh isolated run 20260828T041857Z exited 1 immediately after P3 captures; receipt stops at version_file_address; transcript proves teardown ran: docs/plans/006-worktree-diff/assets/reviews/runtime/out/run-20260828T041857Z/ |
| f-003 | defect | MINOR | MEDIUM — an unreachable-path error does not reset or transactionally stage the prior absence streak. Sequence Ok(false) -&gt; Err(PermissionDenied) -&gt; Ok(false) removes the root even though Ok(false) did not occur on two consecutive scheduled passes, contrary to the service contract. The current error test starts with an empty streak and therefore cannot catch this sequence. Smallest fix: update absence counters in a staged map committed only after a successful planning pass, or explicitly clear that root's streak before returning an exists error; add the three-pass sequence as a unit test. | /Users/jordanknight/substrate/flowspace/fs3-worktree-diff/crates/daemon/src/worktrees.rs:131-150 | — | — | — | — | — | the `?` at lines 133-134 returns while `missing` retains the earlier count; the next false increments it to MISSING_PASSES_BEFORE_REMOVE; `an_unreachable_path_fails_instead_of_being_reaped` covers only Err from an empty map |
| f-004 | defect | MINOR | MEDIUM — first_light compares a raw retrieval score (`best[score] &gt; 0.0`). The packet's measured evidence shows score jitter across identical calls and binds tests to result identity or rank order, never raw score comparisons. This assertion adds no contract coverage beyond the adjacent path/address/kind assertions and can fail on provider geometry rather than product behavior. Smallest fix: delete the positivity comparison or replace it with a type/presence assertion that does not compare the numeric value. | /Users/jordanknight/substrate/flowspace/fs3-worktree-diff/crates/daemon/tests/first_light.rs:450-453 | — | — | — | — | — | exact comparison found by repository search; no other retrieval-score comparison exists in the changed store/daemon/probe tests |
| f-005 | defect | MAJOR | HIGH — the raw representative fix does not apply the caller scope when a smart vector chooses which smart_content.raw_hash it represents. Both smart-content LIMIT 1 selectors are global (the CTE selector is also unordered), while one summary text_hash may legally map to several raw_hashes. A caller-held raw_hash can make the smart vector valid for the scope, yet the oldest foreign raw_hash is chosen and the newly scoped element JOIN drops the hit, under-filling or omitting a valid result before the requested limit. Claim violated: scope filters narrow the candidates the caller can actually open without losing valid caller content. Smallest fix: make the smart-content raw_hash chooser use the same repo/path/worktree anchor predicates before choosing, and add a smart-source fixture with one text_hash mapped to foreign and caller raw_hashes in distinct blobs. | /Users/jordanknight/substrate/flowspace/fs3-worktree-diff/crates/store/src/embeddings.rs:406-415,428-437,466-472 | — | — | — | — | — | transactional isolated-DB proof: caller_eligible_rows=1, chooser_picked_foreign=true, scoped_elements_after_chooser=0; de054e6 test covers SourceKind::Raw only |

<a id="verdict"></a>

## Verdict

| id | finding | resolution | rationale | state | note | links | receipt |
| --- | --- | --- | --- | --- | --- | --- | --- |
| v-001 | [f-001](cross-model-review.dd.md#findings) | fixed | Reproduced independently, then fixed by de054e6: the element representative is caller-scoped and the load-bearing insertion-order fixture matches the failure shape. | — | — | — | focused regression 1/1; the former runtime query now returns 10/10 rows with non-null path/repo/worktree |
| v-002 | [f-002](cross-model-review.dd.md#findings) | fixed | 23a9b93 makes both provider-marker lookups non-fatal and avoids the `[[ ... ]] && ...` set-e trap. Shell syntax passes; PM owns the required complete post-fix probe rerun. | — | — | — | probe fix inspected; bash -n passed; complete post-fix run pending |
| v-003 | [f-003](cross-model-review.dd.md#findings) | fixed | 7061398 handles Err explicitly, clears that root's streak before returning, and adds the exact false -&gt; PermissionDenied -&gt; false sequence; the final false remains observation one. | — | — | — | cargo test -p fs3-daemon --lib worktrees: 11 passed, including an_error_between_absences_resets_the_streak |
| v-004 | [f-004](cross-model-review.dd.md#findings) | fixed | c44b595 removes the numeric score comparison and replaces it with a stable top-hit address identity assertion, while adjacent path/worktree/kind checks remain. | — | — | — | cargo test -p fs3-daemon --test first_light: 14 passed |
| v-005 | [f-005](cross-model-review.dd.md#findings) | fixed | c44b595 rewrites admission to avoid choosing a smart raw_hash, scopes the smart-content chooser through caller-held elements, and orders ties by created_at, model_key, raw_hash. The fixture reproduces one text_hash mapped to foreign-oldest and caller raw bodies and asserts the caller-held smart hit is returned with caller root/path/blob. | — | — | — | cargo test -p fs3-store --test pg_first_light: 19 passed, including smart_search_chooses_the_raw_body_held_by_the_caller |
