{
  "dd": {
    "schema": "builder/plan"
  },
  "sections": [
    {
      "name": "meta",
      "value": {
        "title": "jobs-retention — done jobs are history, not a table the daemon scans every 6 seconds",
        "slug": "jobs-retention",
        "ordinal": 14,
        "status": "ready",
        "summary": "MEASURED ON PROD 2026-09-02 (assets/inputs/db-cpu-profile-report.md §3, §6, §8 cause #2 and #5): the jobs table holds 1,016,092 rows of which 1,009,934 (99.4%) are state='done' — settled history with no retention or purge path anywhere in crates/store/src/jobs.rs or crates/daemon (it doubled during ",
        "backpressure": "assets/backpressure.dd.json#rows"
      }
    },
    {
      "name": "summary",
      "value": "MEASURED ON PROD 2026-09-02 (assets/inputs/db-cpu-profile-report.md §3, §6, §8 cause #2 and #5): the jobs table holds 1,016,092 rows of which 1,009,934 (99.4%) are state='done' — settled history with no retention or purge path anywhere in crates/store/src/jobs.rs or crates/daemon (it doubled during today's batch). `queue_depth()` (jobs.rs:569, `SELECT kind, state, count(*) … GROUP BY kind, state` with NO WHERE) runs a 3-worker Parallel Seq Scan reading 892 MB from disk (`Buffers: shared hit=17 read=114185`, ~260 ms CPU) and is called by report_progress every PROGRESS_EVERY=5 s while working (runner.rs:360/397/429/773) AND by every GET /status (status.rs:30) — 27% of all active DB samples and the dominant source of ~200 MB/s disk read. `jobs_remaining()` by contrast is an index-only scan in 0.77 ms. jobs has NEVER been autovacuumed (n_dead_tup 196,719, 6,549 short of the 203,268 trigger); when it fires it vacuums a 2.15 GB relation with 64 MB maintenance_work_mem across five indexes. Stale doc comment at jobs.rs:558-560 names an index (jobs_claim_idx) that no longer exists. Related: two failed rows can share a dedupe key (row 123) because jobs_live_dedupe_idx is live-only."
    },
    {
      "name": "goals",
      "value": [
        "Progress reporting and GET /status read only LIVE rows (pending/running/failed-non-terminal) via an index-only path in single-digit milliseconds; the full-history GROUP BY is gone from every hot path.",
        "Done jobs are retained for a bounded window (default 1 day, ruled 2026-09-02 in prime-reply-001 item 4; config `indexing.job_retention_days`) and purged by a daemon-owned sweep that runs at boot and on a timer, in batches, with a receipt in the log and in `status`.",
        "The purge is safe: it never touches pending/running/failed rows, never a row younger than the window, and is idempotent; the duplicate-key repair (row 123) is prevented at mint — a failed non-terminal row under a key blocks a second mint.",
        "The one-off prod purge of today's 1.01M done rows is applied by o-prime after merge (Jordan's GO stands: 'restart whenever, get the work done'), with before/after counts, table size, and the /status wall time as the receipt.",
        "The stale comment is fixed and a test pins queue_depth's plan to an index-only scan (no Seq Scan node) so the regression cannot return silently."
      ]
    },
    {
      "name": "non_goals",
      "value": [
        "No change to the claim query or the claim indexes (measured fine).",
        "No change to postgres config (row 140, o-prime applies separately).",
        "No history/analytics table in v1 — a bounded purge is enough; if someone needs done-job history later that is its own row.",
        "No change to job payload shapes."
      ]
    },
    {
      "name": "acceptance_criteria",
      "value": [
        {
          "id": "ac-0001",
          "state": "unchecked",
          "claim": "Hot path is live-only, mutation-checked: `queue_depth` (or its replacement used by progress + /status) produces a plan with NO Seq Scan on jobs against a seeded table of ≥200k done rows — asserted by an EXPLAIN (FORMAT JSON) test; the same test fails against the old GROUP BY (kept as a golden string for the one-line mutation). PR body states the mutation."
        },
        {
          "id": "ac-0002",
          "state": "unchecked",
          "claim": "Retention is bounded and safe: a test seeds done rows across ages, pending/running/failed rows, runs the purge, and asserts exactly the done rows older than the window are gone, live rows untouched, and a second run purges nothing; batch size respected (no single statement over N rows)."
        },
        {
          "id": "ac-0003",
          "state": "unchecked",
          "claim": "Mint refuses/absorbs a duplicate under a failed non-terminal key (row 123 prevention): a test mints a job, fails it non-terminally, mints the same key again, and asserts one row (or a named absorb) — and an absorbed re-fire of a failed row is claimable; red without the fix."
        },
        {
          "id": "ac-0004",
          "state": "unchecked",
          "claim": "/status carries a retention receipt (`retention: {window_days, last_purge_at, purged_last_run}`) and the daemon log names each purge with counts."
        },
        {
          "id": "ac-0005",
          "state": "unchecked",
          "claim": "PROD, after o-prime's bounce and one-off purge: `select state, count(*) from jobs group by 1` before/after; `pg_total_relation_size('jobs')` before/after; `/usr/bin/time -p flowspace3 status --json` ×3 under 200 ms wall (was ~135 ms DB + 260 ms CPU per call plus the disk read); pg_stat_user_tables.seq_tup_read for jobs flat over 5 minutes of daemon operation (was +10.1M per 65 s). Receipt = all four."
        }
      ]
    },
    {
      "name": "phases",
      "value": [
        {
          "id": "ph-4e96",
          "title": "Phase 1: live-only hot path + bounded retention",
          "brief": "Single unit, single coder, no PM: (1) replace the hot-path GROUP BY with a live-only, index-served query; (2) retention config + daemon-owned batched purge at boot and on a timer, with receipt in /status and log; (3) mint-time duplicate prevention; (4) tests incl. two mutations; (5) PR; (6) prod purge + measurements with o-prime after the bounce.",
          "state": "unchecked",
          "tasks": "assets/tasks/phase-1/tasks.dd.json#tasks"
        }
      ]
    }
  ]
}
