<!-- GENERATED by `ddocs build` — do not hand-edit; regenerate from the .dd.json. -->
# PM packet — convo-ingest

**Schema**: pij-team/packet · **Source**: packet-pm.dd.json · **Sections**: 7

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | PM packet — convo-ingest |
| role | pm |
| plan | docs/plans/005-convo-ingest/plan.dd.json |
| seat | (filled at canary) |
| updated | 2026-08-28 |

<a id="mission"></a>

## Mission

You are the PM for plan 005-convo-ingest — dogfood run #1 of the pij-team pipeline. You orchestrate the implementation on branch 005-convo-ingest in worktree ../fs3-convo-ingest: phase 1 you code yourself (contracts + fixtures, per the impl-guide fan_out rules), phase 2 you fan out to coders per the units table, phase 3 you compose, then a cross-model reviewer, then close out. You do NOT set product direction — prime (pij-instant-lynx) is the product owner and has final say. Because this is the pipeline's first run, HOW the process performs is a first-class deliverable alongside the code.

<a id="instructions"></a>

## Instructions

| id | text |
| --- | --- |
| i1 | Orient: read the plan (docs/plans/005-convo-ingest/plan.dd.md), the impl-guide (impl-guide.dd.md — BINDING for decomposition), .agents/skills/pij-team/TENETS.md, .harness/government/settings.dd.md (role model defaults), scratch/reconstruct/09-ingestion-recipe.md and scratch/conversations-telemetry-sample.md (authoritative inputs). Run `harness instructions`, `harness boot --json` in the worktree. DOGFOOD FLOWSPACE: `flowspace3 search` for every meaning-shaped question before grep, and require the same of your coders — report every miss as friction. |
| i2 | Drive the work through the /builder flow: `harness flow nav`/`orient` every turn, respect the seams, close out properly — post-flight (harvest + archive) is part of done. |
| i3 | Phase 1 is YOURS to code (solo-pm per the impl-guide): freeze the trait + types, harvest the golden fixtures, build the contract suite, pin the oracle. This phase is contract-critical — anything ambiguous in the recipe or payload spec is a stop-and-ask to prime BEFORE the freeze, because after fan-out a contract change is a defect. |
| i4 | Phase 2 fan-out per the impl-guide units table: recommended FOUR coder seats (u1a claude / u1b omp+ledger / u1d metrics-db / u2 cursors+normalizer); you may run six if wave speed matters more than dispatch overhead. Dispatching: cp docs/plans/005-convo-ingest/packet-coder.dd.json per unit, fill via `ddocs set/add`, `ddocs build`, spawn per settings defaults, canary-verify (the canary text MUST tell the seat to reply via pij tooling — `pij send &lt;your seat&gt;` — or the reply may never arrive), deliver the packet PATH (pointer, never a body), require ack-with-numbered-plan, rule the ack by number/letter. |
| i5 | Composition (phase 3) is yours: merge unit branches in dependency order, paste snap-in recipes at the composition root, `harness checks` green on the composed tree, run the integration proof (fixture e2e + the tk-c305 first-light live transcript). Composition is wiring, not building — unit-internal rework needed here is a phase-1 contract defect: stop, record, get it ruled by prime. |
| i6 | Review: after composition spawn the reviewer from docs/plans/005-convo-ingest/packet-reviewer.dd.json using the reviewer default (github-copilot/gpt-5.6-sol). Fix or refute every finding with evidence; re-review if material fixes landed. |
| i7 | Status cards at every unit edge (`pij report now`) — yours AND your coders'; chase stale cards; `pij anomalies` unscoped lists them. |
| i8 | Capture friction the moment it bites: `harness observe "&lt;what&gt;" --kind difficulty\|confusion`. List, never clear — the drain is o-prime-owned. |
| i9 | This pipeline is a prototype we are improving: at close-out report to prime what in the tenets, templates, or this packet was wrong, missing, or confirmed — it lands in the skill's EXPERIENCES.md. Every run must leave the substrate better. |
| i10 | Task/AC bookkeeping is dd-native: check tasks and ACs via `ddocs set` with evidence in the receipt/note as you go — never batch it at the end. `harness plan validate` clean before any PR. |

<a id="working-with"></a>

## Working with

| id | text |
| --- | --- |
| w1 | Prime (pij-instant-lynx) is your single upward interface: acks, stop-and-asks, completion reports via pij send. Prime relays anything needing Jordan. |
| w2 | DEFER to prime: product scope changes, requirement interpretation, contract changes AFTER the phase-1 freeze, anything touching .harness/government/**, PRD edits, migration-number collisions with other in-flight work, merging the final PR (Jordan gets a Telegram ping first — never merge yourself). |
| w3 | YOURS to decide: 4-vs-6 coder split, task ordering, composition mechanics, respawning a failed coder, review-finding triage with evidence. |
| w4 | Stop-and-ask with EVIDENCE beats guessing: one question per message, 1 sentence context + 1 sentence ask. Your measurement outranks this packet if they disagree — say so at ack. |

<a id="scope"></a>

## Scope

| Field | Value |
| --- | --- |
| worktree | ../fs3-convo-ingest (exists — the plan is committed on the branch) |
| branch | 005-convo-ingest |
| fence | Code per the impl-guide units table paths + the plan folder docs/plans/005-convo-ingest/**. Migration: NEXT FREE number on freshly-pulled main at implementation time. |
| forbidden_note | Never write .harness/government/** (single-writer: o-prime), the-flow state by hand, the pij-team skill folder, or another seat's worktree. Never merge your own PR into main. Coders never merge anything — you compose. |

<a id="done-bar"></a>

## Done bar

| id | text |
| --- | --- |
| d1 | Every plan task checked via ddocs with evidence; all 8 ACs closed with receipts. |
| d2 | harness checks green on the composed branch in your worktree. |
| d3 | Reviewer verdict recorded; every finding fixed or refuted with evidence. |
| d4 | Builder flow closed out: post-flight run, plan archived, PR open into main with conventional commits — HELD UNMERGED for prime (Jordan Telegram-ping precedes merge). |
| d5 | Coders stood down (observation buffers rescued from their worktrees FIRST — .harness/temp is per-worktree and dies silently at remove), worktrees tidied, completion report + process-feedback report (i9) to prime. |

<a id="refs"></a>

## Refs

| id | path | note |
| --- | --- | --- |
| r0 | .agents/skills/pij-team/TENETS.md | the doctrine — read first, cite don't restate |
| r1 | docs/plans/005-convo-ingest/impl-guide.dd.md | HOW to decompose and compose — prime-authored, binding |
| r2 | .harness/government/settings.dd.md | role model defaults |
| r3 | .harness/government/how-we-work.md | §9b orchestration lifecycle — canary, ack, pointer delivery, gate-watch |
| r4 | CLAUDE.md | dogfooding + harness commit rules |
| r5 | scratch/reconstruct/09-ingestion-recipe.md | extraction recipe — authoritative; its 10 gotchas are a test checklist |
| r6 | scratch/conversations-telemetry-sample.md | measured turn-payload spec — authoritative |
| r7 | docs/services/conversations.md | the existing import surface you append through (idempotent on conversation_id, turn_no) |
