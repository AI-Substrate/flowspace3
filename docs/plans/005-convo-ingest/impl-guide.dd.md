<!-- GENERATED by `ddocs build` — do not hand-edit; regenerate from the .dd.json. -->
# Implementation guide — convo-ingest

**Schema**: pij-team/impl-guide · **Source**: impl-guide.dd.json · **Sections**: 8

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | Implementation guide — convo-ingest |
| plan | docs/plans/005-convo-ingest/plan.dd.json |
| mode | build |
| updated | 2026-08-28 |

<a id="architecture"></a>

## Architecture

Doctrine: .agents/skills/pij-team/TENETS.md — tenets 1-6 govern this document. This repo instantiates them per docs/rules-idioms-architecture/fs3-architecture.md (binding, do not re-litigate): boundaries are crates (arch-allowlist.toml, checked by harness checks); functional core / imperative shell; one composition root (the daemon/CLI config match); testkit fakes over mocks.

Placement for THIS plan (tier rule: pure logic → core · world→types → parsers · port impl → providers · PG → store · wiring → daemon/cli): IngestInput/SourceCursor/RawRecord/ConversationTurn types + the ConversationSource trait in CORE (the trait earns existence with four real impls on day one); the four store readers in PARSERS (they produce core types from the world — a NEW parsers module family conversation_sources/, one module per store); cursor persistence in STORE (a new pg table beside the conversations tables, next free migration number at implementation time); the normalizer in CORE (pure); the orchestrator + CLI verb in the existing daemon/cli wiring; the harness extension in .harness/extensions/convo/. Allow-list grants: parsers → rusqlite (metrics-db reader ONLY — one row); no other new edges expected; any further edge is stop-and-ask.

INTERFACES ARE SETTLED IN PHASE 1, BEFORE FAN-OUT: the trait + types + fixtures + contract suite are the frozen seam. reconvo.py (pinned sha) is the reference oracle — a Rust reader that disagrees with the oracle on a fixture is wrong until ruled otherwise. Authoritative inputs: scratch/reconstruct/09-ingestion-recipe.md (store shapes, cursors, gotchas — treat its 10 gotchas as a test checklist) and scratch/conversations-telemetry-sample.md (payload spec). Contradicting either is stop-and-ask, not silent improvement.

<a id="fan-out"></a>

## Fan out

| Field | Value |
| --- | --- |
| decision | coders |
| rationale | Phase 2 has six genuinely independent units behind a phase-1-frozen trait: four readers (one per store — each a new parsers module, zero shared files beyond one export line each), cursor-state (store), normalizer (core, pure). Recommended seats: FOUR coders, not six — u1c (pij ledger, simplest format) folds into the u1b seat and u3 (normalizer) folds into the u2 seat IF the PM judges either too small for its dispatch overhead (a unit cheaper than its own dispatch is not a unit); PM may run 6 if wave-1 speed matters more than overhead. Phase 1 is solo-PM (serial, small, contract-critical). Phase 3 is the PM composing. |

<a id="units"></a>

## Units

| id | name | paths | responsibility | interface | test_strategy | depends_on | wave | note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| u1a | claude-native reader | crates/parsers/src/conversation_sources/claude.rs (+ one export line), testkit claude fixtures, docs/services/convo-source-claude.md | Claude Code session jsonl + subagent sidecars + spilled tool-results → RawRecords with byte-offset cursors. | impl ConversationSource (frozen phase 1); per-block merge by message.id; record-type allowlist; sidecar re-glob on every poll. | Contract suite over claude fixtures + oracle expectations; sidecar-mid-session and torn-line cases; cargo test -p fs3-parsers, offline. | phase 1 | 1 | Recipe gotchas 1, 6, 9 are this unit's test checklist. |
| u1b | omp-native reader (+ pij-ledger reader) | crates/parsers/src/conversation_sources/{omp,pij_ledger}.rs (+ export lines), fixtures, docs/services/convo-source-omp.md | omp sessions jsonl (xd:// remap, toolCall pairing) and pij events.ndjson (seq cursor, receipts) → RawRecords. | impl ConversationSource ×2 (frozen phase 1). | Contract suite over both fixture sets + oracle expectations; xd:// and compaction-turn cases; cargo test -p fs3-parsers, offline. | phase 1 | 1 | Two impls one seat — ledger is too small for its own dispatch. Recipe gotchas 2, 4, 5. |
| u1d | metrics-db reader | crates/parsers/src/conversation_sources/metrics_db.rs (+ export line), db fixtures, docs/services/convo-source-metricsdb.md | git-ai sqlite metrics (event_kind=5, both dialects incl. copilot) → RawRecords with rowid cursors, repo-scoped. | impl ConversationSource (frozen phase 1); repo scoping non-optional in the public API. | Contract suite over db fixtures; copilot dialect case; scoping proven by API shape test; offline via fixture sqlite file. | phase 1 | 1 | ONLY unit granted a new dependency: rusqlite, one allow-list row. Recipe gotchas 3, 8, 10. |
| u2 | cursor-state service (+ normalizer) | crates/store/src/ingest_cursors.rs + migration, crates/core/src/conversation_normalize.rs, docs/services/convo-cursors.md | Durable cursor persistence (tail buffer, rotation → rescan+dedupe) in PG; and the pure RawRecord → ConversationTurn normalizer to the measured payload spec. | CursorStore trait over PG (fake for tests) frozen phase 1; normalize() pure functions. | In-memory tail/rotation/dedupe tests; PG round-trip in store pg tests; golden in/out pairs for the normalizer with mutation-checked truncation; cargo test -p fs3-store / -p fs3-core. | phase 1 | 1 | Two SRP responsibilities, one seat, zero file overlap between them. Recipe gotcha 7 (live files) + payload spec are the checklists. |
| u4 | orchestrator + CLI + extension (composition) | daemon/cli wiring per composition root, crates/cli conversations ingest verb, .harness/extensions/convo/, docs/services/convo-ingest.md | Join resolver, ingest pipeline (resolve → read → cursor → normalize → idempotent append), CLI envelope, thin harness extension, first-light proof. | Consumes the frozen trait + the units' snap-in recipes; adds no new unit-facing interfaces. | Fixture-backed end-to-end integration test + the LIVE first-light transcript (ingest o-prime's session by pij id, re-poll delta). | u1a, u1b, u1d, u2 | 2 | PM-composed. Composition is wiring: unit-internal rework here = phase-1 contract defect, stop and get it ruled. |

<a id="isolation"></a>

## Isolation

| Field | Value |
| --- | --- |
| mode | worktree-per-coder |
| note | Each coder: own worktree off the plan branch 005-convo-ingest, unit branch merged back by the PM (coders never merge). Fences partition WRITE intent, not the build — wave 1 units share only phase-1-frozen core types, so parallel compiles are safe; if a sibling's tree breaks your build, that is a PM composition concern. Toolchain pinned at phase 1 (rust-toolchain as on main); no unit changes it; done reports carry the observed rustc/cargo versions (tenet 10). |

<a id="composition"></a>

## Composition

| Field | Value |
| --- | --- |
| composer | pm |
| steps | Merge unit branches in dependency order onto 005-convo-ingest → paste each unit's snap-in recipe at the composition root → wire the orchestrator (tk-c302) → harness checks green on the composed tree → run the integration proof. Deliberate checks: mis-wiring (right parts, wrong plumbing — count turns end-to-end vs oracle), interface drift from this doc, envelope drift between normalizer output and import payload, stale doc-comments across fence boundaries. RULE: composition is wiring, not building — unit-internal rework here is a phase-1 contract defect: stop, record (harness observe + skill EXPERIENCES.md), get it ruled. |
| integration_proof | tk-c302's fixture end-to-end test PLUS the tk-c305 first-light transcript: real session ingested by pij id, enrichment drained, hit via search --source conversation, re-poll appends only the delta. |

<a id="review"></a>

## Review

| Field | Value |
| --- | --- |
| when | after phase-3 composition, before the PR into main |
| inputs | plan.dd.md + this impl-guide + full branch diff vs main; priority: plan promises (the 8 ACs), composition seams between the six units, interface drift from this doc, correctness of cursor/rotation logic, contract-test gaps vs the recipe's 10 gotchas |
| model_override | — |

<a id="risks"></a>

## Risks

| id | text |
| --- | --- |
| r1 | Live files written mid-read (torn lines, growing sessions) — retired by the tail-buffer contract-suite case (tk-c104) and gotcha-7 tests. |
| r2 | Machine-wide metrics-db leaking other projects into the index — retired by API-shape-enforced repo scoping (u1d). |
| r3 | Payload drift from the measured spec (oversized turns burning embed budget) — retired by normalizer golden tests pinned to conversations-telemetry-sample.md numbers. |
| r4 | Trait churn after fan-out (the classic parallel killer) — retired by phase-1 freeze + stop-and-ask; any change is a PM ruling recorded in this doc. |
| r5 | Ingesting our own conversations creates feedback loops (the daemon indexing the session that discusses it) — scope: ingest is on-demand pull in v1; watch turn volumes in first light and record findings for the live-capture plan. |
