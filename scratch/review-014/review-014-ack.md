# review-014 ack — cross-model reviewer, plan 014-jobs-retention

- **Reviewer**: Claude (cross-model), worktree `/Users/jordanknight/substrate/flowspace/fs3-review-014`
- **SHA under review**: `cc8da52c2595662664490de7d2b7a120cad95beb` (branch `014-jobs-retention`, PR #98) — named in the packet, detached in my own worktree, so i1b is satisfied and I am not reviewing a moving tree.
- **Brief read in full**: `packet-reviewer.dd.md` + the `.dd.json` source (the rendered `i6`/`owed-1` rows are truncated at 768 chars in the `.md`; I pulled the full text from the JSON). All three owed lists are present — least-confident (owed-1/i6.1), disbelieve-the-receipts (owed-2/i6.2), known-open (owed-3/i6.3). I am not refusing; the brief is complete.
- **Also read before writing this**: `plan.dd.md` (ac-0001..0005), the full `git show --stat` of the sha, `crates/store/migrations/0023_jobs_retention.sql`, the structural read of `crates/daemon/src/retention.rs`, the store/daemon/config hunks of the diff, and `.harness/temp/agent/jobs-retention-pr-body.md` (the receipts I am instructed to disbelieve).
- **Environment**: `docker ps` confirms `flowspace3-db-test` on `5434`. Every DB test will run with `FS3_TEST_DATABASE_URL=postgres://flowspace3:flowspace3@127.0.0.1:5434/flowspace3_test`. I will not touch `:5433` or `:7373`, and I will run **targeted `cargo test`/`cargo nextest`-style invocations only — never a full `harness checks`** (the exclusive gate slot is not mine).

---

## §A — How I will prove or refute each acceptance criterion

Each item states the claim, the experiment, and **what result would refute it** (a proof I cannot fail is not a proof).

1. **ac-0001 — hot path is live-only, mutation-checked.**
   Run `crates/store/tests/pg_jobs_retention.rs::queue_depth_plan_is_live_only_and_never_scans_done_history` against `:5434` and read the exit code, not the prose. Then re-derive independently: seed ≥200k done rows myself, `EXPLAIN (FORMAT JSON)` **the exact production string** `fs3_store::LIVE_QUEUE_DEPTH_SQL` (not a hand-retyped copy — I will assert the constant is what `queue_depth()` actually executes, since a test that explains a *different* string than production runs is a false green), and assert no `Seq Scan` on `jobs` anywhere in the plan tree including under `Gather`/`Parallel`.
   **Refutation attempts**: (i) confirm the *old* unfiltered `GROUP BY` golden really does produce a jobs `Seq Scan` on the same seeded table — the author's mutation #1 (if the golden also comes back index-only, the test proves nothing); (ii) my own mutation — check whether the assertion survives `enable_seqscan=off`/planner nudges or a tiny table, i.e. whether the test passes for the *wrong reason* on a small seed (does the test's own seed size actually force the old plan to Seq Scan?); (iii) verify `jobs_remaining()` — the other hot-path caller — is also live-only, since the diff re-documented it as served by `jobs_live_dedupe_idx`.
   **Verdict false if**: production and the explained string diverge, the golden mutation does not go red, or any hot-path caller still reads done rows.

2. **ac-0002 — retention is bounded and safe.**
   Run `retention_purges_only_aged_done_rows_in_bounded_idempotent_batches` and read the exit code. Then re-derive: seed done rows straddling the window (older/younger by minutes), plus **negatives I choose** — pending, running, failed-terminal, failed-non-terminal, and a done row whose `updated_at` is NULL/never set — sweep, and assert exactly the aged done rows vanished and every negative survived. Second sweep must delete 0.
   **Bounded-ness**: I will not accept "batch size respected" from a row count. I will assert the *statement* is bounded — read the delete SQL and confirm the `LIMIT`/`ctid`-subquery shape, and confirm `sweep_once` loops with `PURGE_BATCH = 10_000` rather than issuing one unbounded `DELETE`. I will check the batch predicate uses `jobs_done_retention_idx (updated_at, id) WHERE state='done'` so each batch starts at the oldest row and does not rescan.
   **Refutation attempts**: seed a table with more than one batch of expired rows and confirm the loop terminates and totals correctly; seed exactly `N == batch` expired rows and check the terminating condition ("until fewer than 10,000 remain") does not leave a residue or spin forever.

3. **ac-0003 — mint refuses/absorbs a duplicate under a failed non-terminal key (row 123 prevention).**
   Run `dedupe_failed_non_terminal_job_absorbs_a_second_mint`. Re-derive: mint → fail non-terminally → re-mint the same key, assert exactly one row, and assert **which** row won and that it is still `failed`/non-terminal (the author's assumption #3), because "one row" is satisfied by a lost job too. Then check the priority-preservation promise still holds on the widened `ON CONFLICT` target (a re-fire must not demote a promoted job).
   **Mutation**: revert the `ON CONFLICT ... OR (state='failed' AND NOT terminal)` predicate to the old pending/running-only form and confirm the test goes red — and that it goes red for the *right* reason (two rows), not an index error.
   **Seam I will not skip**: `requeue_failed` lost its `NOT EXISTS` live-row guard in this diff. I will prove the new partial-unique index genuinely makes that guard redundant — i.e. that `failed→pending` can never collide — including the case where a pre-migration database still holds a failed-non-terminal row *and* a pending row under one key. If that pair can survive migration, `requeue_failed` now aborts a whole statement where it previously skipped.

4. **ac-0004 — /status carries a retention receipt and the log names each purge.**
   Run `crates/daemon/tests/first_light.rs::status_retention_is_receipted_and_history_is_explicit` and `crates/daemon/tests/streaming.rs::status_retention_log_names_completed_purge` against `:5434`. Re-derive the envelope shape myself: assert `retention.{window_days,last_purge_at,purged_last_run}` exist **and** that no pre-existing `/status` JSON key was renamed or dropped (i7: add, do not rename) — I will diff the `StatusReport`/`views/status.rs` key set old-vs-new, and check the CLI renderer and `crates/cli/src/client.rs` still parse an envelope *without* `retention` (it is `Option`), i.e. an older daemon does not break a newer CLI.
   **Receipt integrity**: assert `last_purge_at`/`purged_last_run` are written only after the final batch (the migration comment claims this) and that a mid-sweep crash leaves the previous receipt intact rather than a half-state — I will drive `sweep_once` and inspect `job_retention_state` between batches, or failing that, prove the write ordering from the code path and say so as reasoning, not as an executed proof.

5. **ac-0005 — PROD before/after.**
   **Not mine to execute and I will not touch `:5433`/`:7373`.** o-prime owns the bounce, the one-off purge, and the four measurements. What I *will* judge is whether the mechanism the author hands o-prime can actually produce that receipt: does the boot pass really drain ~1M rows without a hand-written DELETE, and are the four numbers derivable from what the code emits. I will record ac-0005 as **not-yet-provable-by-reviewer** with the preconditions named, rather than marking it true or false. Per owed-1, I will also flag that "two empty sets agreeing proves nothing" applies to the seq_tup_read check — the after-measurement needs a *positive* control (the daemon actually working during the 5 minutes).

---

## §B — Least-confident hunts (owed-1 / i6.1), in the order I will run them

6. **Migration 0023 on a database I dirty by hand.** I will build the dirty states myself — `failed+failed`, `failed+pending`, `running+failed`, `failed(non-terminal)+failed(terminal)`, and a three-way duplicate — apply 0023, and answer three questions with rows, not prose: (a) which row wins under `row_number() ... ORDER BY (state IN ('pending','running')) DESC, id DESC`; (b) **is any job lost** — the backfill sets `terminal=true` on losers rather than deleting, so I will assert loser rows still exist and that no *pending/running* row was ever retired (the `AND state='failed'` guard); (c) the ordering tie-break `id DESC` prefers the newest failed row — I will challenge whether newest-wins is right when the *older* row is the one boot recovery is about to requeue.
7. **Migration atomicity.** `DROP INDEX jobs_live_dedupe_idx;` then `CREATE UNIQUE INDEX ...` — I will confirm the migration runner wraps each migration in a transaction (so a halfway failure cannot leave the old index dropped and the new one absent), and I will force the `CREATE UNIQUE INDEX` to fail on genuinely irreconcilable data to observe what the database is left holding. Also: the new index is created **non-concurrently** over a 1M-row prod table — I will size the exclusive-lock window that implies at first bounce and report it if material.
8. **Broader partial-unique predicate correctness for every status it covers.** Enumerate the full set of `state` values in the schema and prove the predicate `state IN ('pending','running') OR (state='failed' AND NOT terminal)` admits exactly the intended population — in particular that `terminal` is never NULL (a NULL makes `NOT terminal` NULL and silently drops the row out of the index).
9. **Purge racing the runner and boot recovery.** Assert the bounded `DELETE` cannot lock a live row: read the batch's row-selection predicate and confirm it is `state='done'`-filtered *inside* the subquery so no live tuple is ever locked, then run a concurrent claim/requeue against a sweeping loop and check for lock waits or deadlocks. Specifically test the owed-1 case: **can the purge delete a row boot recovery is about to transition (`done→pending` re-fire)?** — I will enumerate every writer that moves a row *out of* `done` and, if one exists, seed that exact race.
10. **Boot pass on ~1M rows.** Judge WAL volume and lock time: 100 sequential 10k-statement deletes at the first bounce. Is it bounded per boot, or does it loop until drained and thereby starve the runner on a cold prod? I will read the loop's terminating condition and the tick cadence (`RETENTION_EVERY_TICKS = 720` × 5 s = 1 h) and say whether the first boot is a one-shot storm or a bounded slice, and recommend a cap if it is the former.
11. **Retention age = `updated_at`.** Find **every** writer of `jobs.updated_at` (`grep` for exact-identifier writes plus `flowspace3 search` for meaning-shaped "job settlement / requeue / terminal flag" callers) and prove settlement (`complete_job`) actually sets it. Two failure modes I will seed: (a) a done row whose `updated_at` was never set at settlement — does the purge see it as infinitely old and delete it immediately, or never delete it; (b) something that touches `updated_at` on an already-done row and *resets* its age, silently extending retention.
12. **The live-only view — enumerate the negatives.** Per owed-1's "seed positives AND negatives": list every state the runner, the reconcile roster, and `/status` consult — `pending`, `running`, `failed` non-terminal, `failed` terminal, `not_before` in the future, parked/held if such a state exists — and prove none went invisible when the `WHERE` clause landed. I will specifically check the diff's removal of the `scanned`/`summarized`/`embedded` `count(..., "done")` fields from `report_progress` (runner.rs): that is a deliberate observability change, and I will judge whether the progress line is still true for an operator or whether the plan promised a capability that quietly disappeared.

---

## §C — Disbelieving the receipts (owed-2)

13. I will re-run every green claimed in `jobs-retention-pr-body.md` myself and record **exit codes**: store retention/plan/dedupe (3), migration 0023 contract (1), daemon `status_retention` (2), runner progress log (1), runner queue snapshots (1), CLI status/history rendering+parsing (4), config validation/default (1), retention cadence (1). A count I cannot reproduce is a finding.
14. I will re-derive every **number**: 1,016,092 / 1,009,934 / 99.4% / 892 MB / 260 ms / 515k-per-day against `assets/inputs/db-cpu-profile-report.md` §3/§6/§8 — the plan's justification, not decoration. The default moved from the plan's **7 days (goal text, `indexing.job_retention_days`)** to **1 day** in `config.rs`; I will locate the ruling that authorises that change, and if there is none, the plan goal and the code disagree and that is a finding.
15. I will perform **both author mutations** (old `GROUP BY` golden; old dedupe predicate) *and* at least one mutation of my own per promise, per i6.2.
16. Per owed-3 I will **read PR #98's CI result** rather than re-reporting a CI-vs-local difference as a code finding without a named cause.

---

## §D — Known-open: zero findings spent here (owed-3)

17. I will report nothing on: backlog rows 122/126/141/124b, row 131 (`harness checks` truncates the failing tail), row 124 (prod tripwire absent-for-unreachable), row 150 (`.serena` litter), DL-007 (cargo package-cache lock), the local-only `:5434` gate, or the one-off prod purge that o-prime runs post-merge.

## §E — Discipline and delivery

18. **Dogfooding**: I will use `flowspace3 search` first for every meaning-shaped lookup in this review (consumers of `queue_depth`, writers of `updated_at`, the reconcile roster), grep only for exact identifiers, and file any miss with `harness observe --kind difficulty` **plus** a `pij send pij-binding-magpie` while still in context. I will **capture but never drain** the shared observation buffer.
19. **Fence**: read-only on code; I write only `.harness/temp/agent/` and `docs/plans/014-jobs-retention/assets/reviews/` in my worktree. No edits, no commits, no PRs, no government files. No full `harness checks`.
20. **Deliverable**: a ddoc review record in `assets/reviews/`, built, plus `.harness/temp/agent/review-014-verdict.md`, with every AC judged true/false/not-provable-by-reviewer with cited evidence, composition seams reported explicitly (§B items 9, 12, and the `requeue_failed` guard removal in item 3), material findings only with severity + exact location + claim + proof + smallest fix. `no_material_findings` is a verdict I will use if the evidence earns it. Path + one-line verdict sent to `pij-binding-magpie`.

**Standing by for the o-prime ruling on this ack before executing §A–§C.**
