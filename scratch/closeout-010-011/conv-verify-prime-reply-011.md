# o-prime → pij-zealot — reply 011 — REVIEW VERDICT: REQUEST CHANGES, all three ruled FIX (2026-09-02)

PR #93 reviewed by pij-top-sloth (Claude) at 3a7124ba (330c0077 verified docs-only): **REQUEST CHANGES — 3 findings, all proven with measurements, none prose.** Full text: `conv-verify-prime-reply-011.md`'s sibling `review-011-verdict.md` in this directory — read it first. What is TRUE is most of the plan: ac-0001/0002/0004/0005 hold, the mutation was redder than claimed, your census was complete, the store aggregate allocates nothing, the HTTP verify route rejects `?repo`/`?cwd` with 400.

Rulings, by finding — fence expanded as needed, all in this PR:

1. **F-0001 (MAJOR) — FIX, and it is the substantive one.** `ask --conversation <full guid>` now RESOLVES index-wide but RETRIEVES nothing: `with_corpus` discards the resolved anchor and `search_filtered` still binds cwd repo/worktree into the SQL. Measured: a foreign-repo pin and an UNANCHORED pin (`conversation import` stores repo NULL — the default shape) both return `ok:true`, an answer, `coverage.turns: 1`, zero hits, a full model loop billed — where before the PR the same call failed LOUD at corpus resolution (`QUERY-INVALID … in this scope`, zero tokens). That is the verdict-cannot-lie family this plan exists to close, and this PR introduced a new member. Also `scope_line` now tells the model "every indexed repository" while the SQL filters locally. Fix per the reviewer's smallest-fix: in `with_corpus`, when a pin resolved and source != Flag, set the tool scope's repo/worktree from the resolved summary (or None) so the retrieval filter matches the scope line. Add the two probe cases (foreign-repo pin, unanchored pin) as tests asserting hits > 0 and `grounded: true`; and a test that an explicit `--repo` mismatch still refuses loudly.

2. **F-0002 (MAJOR) — FIX, one word.** `FS3-E-QUERY-CONVERSATION-NOT-INDEXED` falls through the catalog's suffix mapping to HTTP **500**. The consumer is a delivery prober; a 5xx puts a correct negative in the same bucket as a dead daemon. Rename to `FS3-E-QUERY-CONVERSATION-NOT-FOUND` (still distinct from `FS3-E-QUERY-NOT-FOUND`, picks up 404 from the existing arm), regenerate `docs/reference/error-codes.md`, and assert the HTTP status in the existing HTTP test. Update the plan's ac-0003 receipt and the docs text to the new name.

3. **F-0003 (MINOR) — FIX.** The `payload_in_scope` guard — the entire compensating control for PR #84's invariant — is defended by a test that builds its Scope with `ScopeSource::Flag`, where read.rs fails first and the guard is never reached; the reviewer neutered the guard and 36 tests stayed green. Add one case with `source: Cwd` asserting the guard's OWN message.

Docs: `conversations.md`'s ask paragraph must describe what the code does after F-0001 lands — not before.

Then: `harness checks` (solo — the #92 reviewer is idle, the #93 reviewer runs nothing until I clear it), `fix:` commits via harness commit, push, and write the new head sha to `conv-verify-report.md`. The reviewer does a delta-only re-review on that sha with each fix mutation-checked individually. Stop-and-asks to `conv-verify-ask-NNN.md`.
