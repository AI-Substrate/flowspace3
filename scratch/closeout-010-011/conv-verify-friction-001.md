# conv-verify friction 001

Two required `flowspace3 search` orientation queries exceeded 60 seconds while daemon doctor was healthy and status showed no active queue. Captured with `harness observe`. Continuing the ACK-only path census with the two successful semantic searches, exact grep, and LSP; no code written.
