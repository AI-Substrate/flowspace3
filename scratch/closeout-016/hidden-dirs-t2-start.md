# hidden-dirs t2 start

Thread `include_hidden = global OR per-root` into initial/re-add scans and watcher rescans. Add fixture coverage for opted-in/default roots, `.hidden/a.ts`, `src/b.ts`, and permanent `.git`/deny-list exclusions. Take independent call-site mutation receipts.
