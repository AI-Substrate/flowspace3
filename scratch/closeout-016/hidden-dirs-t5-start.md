# hidden-dirs t5 start

Scratch config: `/tmp/fs3-hidden-dirs.gis4B3/config.toml`.
Dedicated test database: `127.0.0.1:5434/flowspace3_test` only.
Scratch daemon: `http://127.0.0.1:63624`; no default config or production port/database.
Target root: `/Users/jordanknight/pi-hacking/pij`, currently persisted `include_hidden=false` with no `.pi/%` worktree files.
Named search target: first line `export function daemonLocation(` from `.pi/extensions/pij/adapters/daemon-http.ts`.
