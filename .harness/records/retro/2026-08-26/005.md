---
record_kind: "retro"
harness_version: "0.13.0"
branch: "main"
repo: "https://github.com/AI-Substrate/flowspace3.git"
created_at: "2026-08-26T04:50:12.053Z"
agent: "pij-bitter-gibbon"
plan_id: "001-fs3-foundations"
schema_version: "1.2"
retro_id: "2026-08-26T04:50:12Z-pij-bitter-gibbon-s001"
started_at: "2026-08-26T02:04:00Z"
ended_at: "2026-08-26T04:51:00Z"
summary: "s001 phase-1 (fs3 foundations) driven end-to-end via /pij pair: substrate commit, coder fleet through four review cycles (rev-0001..0006, final APPROVE with Dim-0 mutation gates every cycle), crates/ layout ruling executed, direct-on-main with push-as-you-go, ledger reconciled to validate --complete 0/0/0. Ten frictions captured; two vacuous test passes caught by mutation gating before they shipped."
entries:
  - id: DL-001
    kind: "difficulty"
    description: "omp duplicate self-registration recurred: worker process pid 12424 (canaried as pij-sure-kazimir, pane %33) also holds registry identity pij-shallow-dog which sent a work message — same defect class as s001 DL-001. Phantom NOT closed (shared pid). Platform-level pij/omp bug."
    target: "tooling"
    severity: "None"
    workaround: "None"
    suggested_encoding: "None"
    fp: "1b6cb8cc0afe"
    disposition: "kept"
    system:
      compound:
        status: "open"
        source: "agent-self"
        first_seen_at: "2026-08-26T04:04:44.366Z"
  - id: DL-002
    kind: "difficulty"
    description: "Fleet peers (omp coder/reviewer) reply without loading the pij skill — raw JSON blobs, no wire discipline, no skill conventions; Jordan flagged it directly ('they keep not using pij skill to reply')"
    target: "tooling"
    severity: "degrading"
    workaround: "Restating reply-format contract explicitly in every dispatch message"
    suggested_encoding: "Spawn task text should include a standing line: 'Before your first reply, load skill://pij and follow its C10 wire discipline for every message'"
    fp: "9b02d76e95de"
    disposition: "kept"
    system:
      compound:
        status: "open"
        source: "agent-self"
        first_seen_at: "2026-08-26T04:09:13.626Z"
  - id: DL-003
    kind: "difficulty"
    description: "omp duplicate self-registration 3rd occurrence: spawn s1787717630164-41989 (canaried pij-recent-cicada) also ready-pinged as pij-likely-mosquito"
    target: "tooling"
    severity: "None"
    workaround: "None"
    suggested_encoding: "None"
    fp: "559b26429890"
    disposition: "kept"
    system:
      compound:
        status: "open"
        source: "agent-self"
        first_seen_at: "2026-08-26T04:16:30.894Z"
  - id: DL-004
    kind: "difficulty"
    description: "add-provider skill mandates offline stub-server tests ('axum bound to 127.0.0.1:0 is the pattern') but no stub server exists anywhere in the repo — openai.rs ships only inline unit tests. Every adapter worker re-invents the same ~110-line axum recorder."
    target: "tooling"
    severity: "degrading"
    workaround: "Wrote a StubServer fake inside crates/providers/tests/azure_openai_stub.rs"
    suggested_encoding: "Promote StubServer into fs3-testkit as a shipped fake (rule 5's own home) so adapter N+1 gets request-capture for free; then the skill can say 'use fs3_testkit::StubServer' instead of describing one"
    fp: "fa326f83c71e"
    disposition: "kept"
    system:
      compound:
        status: "open"
        source: "agent-self"
        first_seen_at: "2026-08-26T04:16:41.077Z"
  - id: DL-005
    kind: "difficulty"
    description: "order_embeddings + parse_summary are private in openai.rs, yet every OpenAI-shaped adapter (Azure, OpenAI-compatible generic, Ollama-compat, vLLM/LM Studio — 4 of 6 remaining roster rows) needs them verbatim. The skill's parallel-worker fence ('two workers never touch the same file except lib.rs') actively forces the duplication."
    target: "tooling"
    severity: "degrading"
    workaround: "Copied ~90 lines into azure_openai.rs; referenced only the public OpenAiSummarizer::SYSTEM_PROMPT/user_prompt to avoid a second prompt"
    suggested_encoding: "Add crates/providers/src/wire.rs owned by the integrating stream, holding the OpenAI wire shapes (order_embeddings, parse_summary, Secret); skill gains a 'shared wire helpers live in wire.rs, never re-derive them' line"
    fp: "44eeeea4a22d"
    disposition: "kept"
    system:
      compound:
        status: "open"
        source: "agent-self"
        first_seen_at: "2026-08-26T04:16:50.421Z"
  - id: DL-006
    kind: "difficulty"
    description: "azure_identity's AzureCliCredential shells out to 'az' and caches nothing, so an uncached adapter spawns one process per embedding batch. Nothing in the add-provider skill mentions that acquired credentials need caching."
    target: "tooling"
    severity: "annoying"
    workaround: "Added a Mutex<Option<AccessToken>> cache with a 300s refresh skew plus two stub tests (reuse + refresh)"
    suggested_encoding: "Skill step 2 gains: 'if the credential is acquired rather than read (Entra, STS, OIDC), cache the token with a refresh skew and prove reuse in the stub tests'"
    fp: "5b628a4d11f3"
    disposition: "kept"
    system:
      compound:
        status: "open"
        source: "agent-self"
        first_seen_at: "2026-08-26T04:16:57.133Z"
  - id: DL-007
    kind: "difficulty"
    description: "The add-provider skill's 'Files you create/touch' list omits crates/providers/Cargo.toml and the root Cargo.toml, but any adapter with a new dependency must edit both — and both are shared-merge files exactly like lib.rs."
    target: "tooling"
    severity: "annoying"
    workaround: "Edited both plus crates/testkit/arch-allowlist.toml, which the skill does mention"
    suggested_encoding: "Add both manifests to the skill's file list with the same 'one line each, trivial merge' note lib.rs carries"
    fp: "e7cca9c4970c"
    disposition: "kept"
    system:
      compound:
        status: "open"
        source: "agent-self"
        first_seen_at: "2026-08-26T04:16:57.272Z"
  - id: DL-008
    kind: "difficulty"
    description: "Brief assumed schema setup was ad-hoc inline SQL; the tree already had crates/store/migrations/0001_elements.sql + sqlx::migrate! + a pub migrate(). Two of five brief items were already done. Cost a scout round before real work started."
    target: "tooling"
    severity: "annoying"
    workaround: "Spawned two read-only scouts to map crates/store and daemon boot before touching anything; escalated the two findings to the coordinator in one message rather than one at a time."
    suggested_encoding: "A 'harness capabilities' style query, or a docs/how/database.md that exists BEFORE the second agent arrives, would have answered 'is there a migration story yet' in one command instead of a repo survey."
    fp: "9e6ac4f2dac1"
    disposition: "kept"
    system:
      compound:
        status: "open"
        source: "agent-self"
        first_seen_at: "2026-08-26T04:23:23.430Z"
  - id: DL-009
    kind: "difficulty"
    description: "connect_lazy() had no acquire_timeout while connect() did, so the first use of an absent store waited sqlx's 30s default before erroring - directly against the doctrine in lib.rs:27-32 that CONNECT_TIMEOUT exists to refuse exactly that silence. Only visible by manually smoke-running the daemon binary with a bad URL and timing it."
    target: "tooling"
    severity: "degrading"
    workaround: "Added .acquire_timeout(CONNECT_TIMEOUT) to connect_lazy so the two constructors agree; measured 30.0s -> 5.6s."
    suggested_encoding: "A check that pool constructors in fs3-store share their timeout settings, or simply: no test asserts how LONG an unreachable-store failure takes. A 'fails within N seconds naming the fix' assertion would have caught this without a stopwatch."
    fp: "d69b39e6a386"
    disposition: "kept"
    system:
      compound:
        status: "open"
        source: "agent-self"
        first_seen_at: "2026-08-26T04:23:23.573Z"
  - id: DL-010
    kind: "difficulty"
    description: "harness boot reported cargo build failed without compiler diagnostics"
    target: "tooling"
    severity: "degrading"
    workaround: "run targeted cargo command to obtain diagnostics"
    suggested_encoding: "include bounded cargo stderr in boot error evidence"
    fp: "1b0ae8ab278b"
    disposition: "kept"
    system:
      compound:
        status: "open"
        source: "agent-self"
        first_seen_at: "2026-08-26T04:35:43.296Z"
---

# Retro — s001 fs3-foundations phase exit

